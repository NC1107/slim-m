# SPDX-License-Identifier: Apache-2.0
name: slimm_api
description: Typed client for the slim-m wire protocol, matching schema/openapi.yaml.
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  http: ^1.2.0
  web_socket_channel: ^3.0.0
dev_dependencies:
  fake_async: ^1.3.3
  flutter_lints: ^5.0.0
  test: ^1.25.0
