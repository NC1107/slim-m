<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The debug/profile manifests each carry their own copy of this,
         commented there as being "required for development" (hot reload).
         It is really just required, full stop: this app talks to its home
         server over HTTP and WebSocket, and a release build with no copy of
         this permission in the main manifest cannot reach the network at
         all. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ (API 33) shows nothing without this, declared here and
         requested at runtime (see LocalNotifications in packages/platform);
         a no-op on every older API level, where notification permission is
         granted at install time. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Voice has never worked on Android without this: a call publishes a
         microphone track, and Android refuses the capture outright when the
         permission is not declared. It was missing from the day voice
         shipped, which went unnoticed because no Android device has ever
         been available to test a call on. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Declared alongside the camera pre-toggle this change adds; without
         it the toggle would appear to work and capture nothing. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:label="slim-m"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Invite links: slimm://join?... opens the app on the join flow (see lib/src/deep_links.dart). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="slimm" android:host="join"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
