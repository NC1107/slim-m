# SPDX-License-Identifier: Apache-2.0
name: slimm_platform
description: Per-OS integration behind abstract interfaces (push, calls, notifications).
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_lints: ^5.0.0
