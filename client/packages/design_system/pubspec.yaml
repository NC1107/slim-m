# SPDX-License-Identifier: Apache-2.0
name: slimm_design_system
description: slim-m design tokens, theme, and icon wrappers.
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_lints: ^5.0.0
flutter:
