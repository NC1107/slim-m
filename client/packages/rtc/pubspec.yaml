# SPDX-License-Identifier: Apache-2.0
name: slimm_rtc
description: LiveKit room and client wrapper behind an explicit public API.
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"
dependencies:
  flutter:
    sdk: flutter
  # Pinned exactly: broadcast_bridge.dart reaches into its unversioned `src/`
  # BroadcastManager, so a point release could break iOS screen sharing with no
  # API change. broadcast_bridge_test.dart guards that surface; bump both together.
  livekit_client: 2.10.0
  flutter_webrtc: ^1.6.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  pub_semver: ^2.2.0
  # Already a dev dependency of the api and app packages, at the same version.
  fake_async: ^1.3.3
