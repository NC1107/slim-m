# SPDX-License-Identifier: LicenseRef-PolyForm-Noncommercial-1.0.0
#
# Files already past the 500-line hard limit when the budget gate was written,
# one per line as "<path> <lines> # why".
# The number is the count it was listed at and the gate treats it as that
# file's own ceiling, so this is a debt register rather than an exemption.
# Nothing here is a decision that the file is fine; see docs/ci.md.
#
# Keep each reason to one line. The rationale for a raise belongs in the commit
# that raises it; a changelog kept here makes one physical line that every change
# to the same file must edit, which is a merge conflict by construction.

schema/openapi.yaml 11637 # then +158 for the /voice/webhook route, the LiveKitWebhookEvent schema, the VoiceParticipantJoined/VoiceParticipantLeft/VoiceScreenShareChanged event schemas and their ServerFrame refs, and the is_sharing_screen/has_video fields on VoiceRosterParticipant (decision 0032), then +202 for the setBotCommands/getBotCommands/listChannelBotCommands routes and the BotCommandInput/SetBotCommandsRequest/RegisteredBotCommand/BotCommandRegistration/ChannelBotCommand schemas (bot command registration, decision 0031), then +97 for the Embed/EmbedField schemas and the embeds field on Message (decision 0030's stage 3, read side only: message_embeds enriched onto the DTO, no send-path caller yet), then +107 for the RequestEmbed schema and the embeds field on SendMessageRequest/IncomingWebhookPayload, honouring what stage 3 (read side) only documented (decision 0030), then +92 for the setBotPermissions route, the SetBotPermissionsRequest schema, the permissions/role_id fields on Bot and CreateBotRequest, the managed_bot_id field on Role, and moderationHistory's VIEW_MODERATION_HISTORY branch (decision 0028's declared-permissions/managed-role/ADMINISTRATOR amendment), then +130 for the launchAppMessage and listApps routes and the App/AppSurface/LaunchAppRequest schemas plus the app_surface field on Message (the apps launcher), then +44 for the CallRecord schema and the call field on Message (a DM call's record in the transcript), then +10 for the restricted field on Channel (the private-channel indicator), then +3 for the /metrics description mentioning the request-latency histogram and SQLite pool gauges, then +23 for the LinkPreview author_name/author_url fields and the getLinkPreview/video_provider description updates covering the oembed request, then +24 for the code-runner broker's reserved module_id documented in prose across runModuleCommand/runCodeBlock/listCodeBlockRunners (decision 0026; no new route or schema), then +23 for the markUnread route and the manually_unread field on ReadState (mark a channel unread without rewinding the monotonic read marker), then +138 for the listBots/createBot/revokeBot routes and the Bot/CreateBotRequest/NewBot schemas (bot accounts, decision 0028), then +12 for the is_bot field on UserProfile (the BOT badge), then +45 for the deleteMemberAccount route (an administrator ending somebody else's account, which a removal deliberately does not do), then +10 for listMembers' channel parameter (the member pane's roster, narrowed to a channel's viewers), then +14 for the restricted field on CreateChannelRequest and its createChannel description update (the private-at-creation flag, gated on MANAGE_ROLES as well as MANAGE_CHANNELS), then +30 for the parent_content/parent_deleted/parent_author_id/parent_author_display_name fields on ThreadParent (a cold-opened thread panel can now render the message it hangs off), then +179 for the deliverWebhook route and the IncomingWebhookPayload/DeliveredWebhookMessage schemas, the is_webhook field on UserProfile, and the webhooks entry in Version.capabilities' enum (incoming webhooks, decision 0030, stages 1-2), then +24 for the CodeRunsCleared schema and its ServerFrame ref (an edit invalidates a message's shared code-run output), then +7 for runCodeBlock's description documenting that an app surface's own moduleId/command is now enforced on every block index (no new route or schema; a security fix)., then +14 for listBots' include_removed query parameter (a bot removed from the Space is left out of the default list; see docs/decisions/0028-bot-accounts.md), then +115 for the reorderRoles route and ReorderRolesRequest schema, the position/member_count fields on Role, and the batchSetChannelOverwrites route and BatchOverwriteEntry/BatchSetOverwritesRequest schemas (the roles-and-permissions redesign's reorder and grid-save endpoints), then +191 for the createWebhook/listWebhooks/renameWebhook/revokeWebhook routes and the Webhook/CreateWebhookRequest/NewWebhook/RenameWebhookRequest schemas (webhook admin surface, decision 0030's admin-surface addendum), then +26 for the getMessage route (GET /channels/{channelId}/messages/{messageId}), returning the same enriched Message shape listMessages does over a single id, then +86 for pronouns/about/profile_color on UserProfile/Me/UpdateMeRequest and client_kind/client_version on RegisterRequest/LoginRequest/Device (the profile-first member card and named devices), then +18 for the MemberJoined schema and its ServerFrame ref (a real join event, so a greeter bot does not have to guess). One OpenAPI document is one document; splitting it across $ref files would give tests/openapi_contract.rs two sources to reconcile. This number rises when the API does, and the raise is the deliberate act; why each raise happened is in the commit that made it, not here.
.github/workflows/release.yml 840 # ten publish jobs sharing release-please outputs, plus two thin verify jobs wiring every publish path to one commit-status gate; why each raise happened is in the commit that made it, not here. +4 for the release-asset uploads taking the same token release-please creates the release with; once RELEASE_PLEASE_TOKEN was set the release was owned by the PAT and the Actions token could no longer update it.
.github/workflows/main-builds.yml 505 # one continuous-deploy workflow whose jobs share a concurrency group, outputs and the server-image-needed decision; splitting it would split the trigger and the cancel-in-progress semantics docs/ci.md documents. Crossed 500 adding the server-affecting root paths server-ci.yml already listed, so a lone Cargo.lock or Dockerfile bump redeploys. Rises when a job or trigger is added. +3 for the client release PR's app pubspec exclusion and the CONTINUOUS_TESTFLIGHT gate on ios-testflight, so a client release ships one TestFlight build rather than two.
crates/slimm-server/src/store/sessions.rs 632 # sign-in, sign-out, ws tickets and the token sweep; refresh rotation was split to store/refresh_rotation.rs; +6 for open_session_as taking a client kind/version and the thin open_session wrapper kept for the many callers with no client to report
client/packages/app/test/push_controller_test.dart 928 # one suite over registration, lifecycle reporting and revocation; new push coverage goes in a sibling file rather than growing this one
crates/slimm-server/tests/invites.rs 678 # issue, check, redeem and revoke, plus the four-answers-are-identical assertions; +1 for the AppState.dock field the modules-foundation change added to every fn app(), then +1 for AppState.code_runner (decision 0026's broker)
client/packages/api/test/new_routes_test.dart 596 # the REST routes of PR #36 in one file; the WebSocket frames split to new_routes_ws_frames_test.dart
crates/slimm-server/tests/polls.rs 605 # create, vote, close and the permission matrix; +1 for the AppState.dock field the modules-foundation change added to every fn app(), then +1 for AppState.code_runner (decision 0026's broker)
crates/slimm-server/tests/dms.rs 594 # pair membership, the deliberately skipped ADMINISTRATOR bypass, and fan-out; +1 for the AppState.dock field the modules-foundation change added to every fn app(), then +1 for AppState.code_runner (decision 0026's broker)
crates/slimm-server/tests/overwrites.rs 531 # set/clear/list plus the escalation and refuse-identically matrix in one file; the listing tests would need the shared harness extracted to split cleanly; +1 for the AppState.dock field the modules-foundation change added to every fn app(), then +1 for AppState.code_runner (decision 0026's broker)
client/packages/app/test/session_persistence_test.dart 588 # restore, refresh and revocation paths
crates/slimm-server/tests/message_endpoints.rs 537 # send, list, edit and delete with their permission cases; +1 for the AppState.dock field the modules-foundation change added to every fn app(), then +1 for AppState.code_runner (decision 0026's broker)
client/packages/app/lib/src/widgets/composer.dart 686 # then +20 wiring channel-scoped bot command discovery into suggestions and the mentioner-only help card (decision 0031); autocomplete, focus, paste and drop-target registration together; +25 wiring module slash commands into send, then +33 wiring the apps launcher into send and the actions menu (the launch itself lives in app_launcher_sheet.dart); +36 the Up-in-empty-composer key check and its own mounted-guarded, meProvider-awaiting lookup, still not safe to split the State class under an unrelated change
crates/slimm-server/src/hub/event.rs 586 # one exhaustive Event enum; a new broadcast (CodeRunChanged, for shared code-block output) grows it by one variant and cannot be split out of the enum. Rises when the protocol adds an event, like openapi.yaml above. +16 for MessageCreated's app_surface/code_run/poll fields, so a live frame can carry what the REST path already enriches. +13 for CodeRunsCleared (an edit drops a message's stale code-run output). +2 for MessageCreated's embeds field (decision 0030). +23 for VoiceParticipantJoined/VoiceParticipantLeft/VoiceScreenShareChanged (decision 0032). +15 for MemberJoined, so a greeter bot has a real join event instead of the client guessing.
crates/slimm-server/src/http/ws/authorization.rs 582 # the exhaustive per-connection frame authorization + build match; a new event adds one arm here and cannot be split from the match. Rises when the protocol adds an event. +8 destructuring and forwarding MessageCreated's new app_surface/code_run/poll fields. +9 for CodeRunsCleared. +5 threading link_previews through to resolve an embed's image token (decision 0030). +48 for VoiceParticipantJoined/VoiceParticipantLeft/VoiceScreenShareChanged's extra_bit, channel_id, presence-visibility gate and to_frame arms (decision 0032). +4 for MemberJoined's Deliver arm and its three exhaustive-match placeholders.
crates/slimm-server/src/hub.rs 501 # two exhaustive event classifiers (moves_permissions, is_ephemeral) that gate the durable/ephemeral channel split and the permission-cache epoch; a new event adds one arm to each and cannot be split from the match without splitting the classifiers apart from the channel they gate. Rises when the protocol adds an event. +4 for MemberJoined in both classifiers plus its `channel_traffic_leaves_it_alone` test case.
crates/slimm-server/src/http/auth.rs 501 # register/login/refresh/ticket/logout/delete_account plus their inline validation unit tests; already at the 500-line ceiling before this change. +1 for MemberJoined published on a fresh registration, the real join path a greeter bot needs.
client/packages/api/lib/src/events.dart 553 # the client's exhaustive ServerEvent.parse switch; a new frame (code_run.changed) adds one arm. The frame classes already live in the events_frames.dart part; the parse switch itself must stay whole. +7 for code_runs.cleared. +38 for voice.participant_joined/voice.participant_left/voice.screen_share_changed (decision 0032). +3 for member.joined.
