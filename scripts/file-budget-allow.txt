# SPDX-License-Identifier: Apache-2.0
#
# Files already past the 500-line hard limit when the budget gate was written,
# one per line as "<path> <lines> # why".
# The number is the count it was listed at and the gate treats it as that
# file's own ceiling, so this is a debt register rather than an exemption.
# Nothing here is a decision that the file is fine; see docs/ci.md.

schema/openapi.yaml 5546 # one OpenAPI document is one document; splitting it across $ref files would give the router-vs-schema gate two sources to reconcile. This number rises when the API does; raising it is the deliberate act, and the diff is where that shows. Raised again for GET/PUT /push/preference, the account-wide notification preference
.github/workflows/release.yml 809 # ten publish jobs sharing release-please outputs, plus two thin verify jobs wiring every publish path to a required commit-status gate; each publish job's own condition collapsed into that gate's result, so the release condition lives in one place per component rather than eight. Raised for the second release-please-action step: one manifest file per package instead of one shared between both, so a release commit for either package can no longer conflict the other's standing PR
crates/slimm-server/src/store/sessions.rs 815 # tokens, refresh rotation, ws tickets and account deletion in one file; account deletion is the obvious first split
crates/slimm-server/src/push.rs 600 # envelope sealing, relay client and fan-out triggering; the sealed-box half has no dependency on the other two
client/packages/app/test/push_controller_test.dart 925 # one suite over registration, lifecycle reporting and revocation
crates/slimm-server/tests/push_endpoints.rs 692 # registration, listing and revocation of push devices
crates/slimm-server/tests/invites.rs 675 # issue, check, redeem and revoke, plus the four-answers-are-identical assertions
client/packages/api/test/new_routes_test.dart 666 # the 21 routes of PR #36 in one file, named for that pull request rather than for anything it covers
crates/slimm-server/tests/polls.rs 602 # create, vote, close and the permission matrix
crates/slimm-server/tests/dms.rs 591 # pair membership, the deliberately skipped ADMINISTRATOR bypass, and fan-out
client/packages/app/test/session_persistence_test.dart 588 # restore, refresh and revocation paths
crates/slimm-server/tests/message_endpoints.rs 534 # send, list, edit and delete with their permission cases
