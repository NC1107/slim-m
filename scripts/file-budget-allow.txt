# SPDX-License-Identifier: Apache-2.0
#
# Files already past the 500-line hard limit when the budget gate was written,
# one per line as "<path> <lines> # why".
# The number is the count it was listed at and the gate treats it as that
# file's own ceiling, so this is a debt register rather than an exemption.
# Nothing here is a decision that the file is fine; see docs/ci.md.

schema/openapi.yaml 6364 # one OpenAPI document is one document; splitting it across $ref files would give the router-vs-schema gate two sources to reconcile. This number rises when the API does; raising it is the deliberate act, and the diff is where that shows. Raised for canvas.cursor.moved, GET/PATCH /space/analytics, the image kind plus the move op on the canvas, the reorder op and canvas.object.reordered event, the note and shape object kinds, canvas.stroke_preview (client) plus canvas.stroke_preview.updated (server), and now GET/PUT canvas/media-slots plus canvas.media_slot.changed. Raised two lines, not eight: putCanvasMediaSlot gained a genuine new response (404 for a userId naming no account), and every other line of prose the same change first added was trimmed back into the existing paragraphs at net zero, proven by rewrapping rather than assumed. Raised 45 lines for GET /channels/{channelId}/permissions and its ChannelPermissions schema, the first step of docs/decisions/0011-per-channel-permissions.md. Raised 27 lines for step 2: Channel.permissions and Report.channel_permissions. Raised 50 lines for GET /channels/{channelId}/thread-parent and its ThreadParent schema, closing the screen-review finding that a cold-opened thread panel had nothing to resolve its own parent channel's name from. Raised 17 lines for RegisterPushRequest.include_content, the per-device opt-in to a message preview sealed inside the push envelope; the description carries the trust boundary (the relay cannot read it) because that is the whole reason the field is safe to offer Raised 17 lines for the MemberRestored event schema, the mirror of MemberRemoved a remove-then-restore needs to reach connected clients. Raised 31 lines for GET /metrics: the Prometheus-exposition route the observability job added, gated on MANAGE_SERVER like /space/analytics rather than left open like /version.
.github/workflows/release.yml 817 # ten publish jobs sharing release-please outputs, plus two thin verify jobs wiring every publish path to a required commit-status gate; each publish job's own condition collapsed into that gate's result, so the release condition lives in one place per component rather than eight. Raised for the second release-please-action step: one manifest file per package instead of one shared between both, so a release commit for either package can no longer conflict the other's standing PR. Raised one line for the gstreamer dev headers audioplayers_linux needs, without which the linux artifacts silently do not build. Raised four lines for the notification service extension's provisioning profile: a third separately-signed bundle needs its own secret, its own decode and its own presence check, and without all three the iOS job stops shipping anything at all rather than failing loudly. Trimmed back to four from six first - the decode is one line like the app's own and the comment it sits under was reworded rather than grown. Raised three lines to pin flutter-version 3.44.9 on each of the three flutter setups, one per store artifact, after an unpinned stable channel broke a release mid-flight
crates/slimm-server/src/store/sessions.rs 815 # tokens, refresh rotation, ws tickets and account deletion in one file; account deletion is the obvious first split
client/packages/app/test/push_controller_test.dart 928 # one suite over registration, lifecycle reporting and revocation. Raised 3 lines: every registration now reads the content-preview setting, which needs SharedPreferences mocked or a fakeAsync test hangs rather than throwing; the new include_content coverage itself lives in push_controller_content_preview_test.dart instead of growing this file further
crates/slimm-server/tests/invites.rs 675 # issue, check, redeem and revoke, plus the four-answers-are-identical assertions
client/packages/api/test/new_routes_test.dart 666 # the 21 routes of PR #36 in one file, named for that pull request rather than for anything it covers
crates/slimm-server/tests/polls.rs 602 # create, vote, close and the permission matrix
crates/slimm-server/tests/dms.rs 591 # pair membership, the deliberately skipped ADMINISTRATOR bypass, and fan-out
client/packages/app/test/session_persistence_test.dart 588 # restore, refresh and revocation paths
crates/slimm-server/tests/message_endpoints.rs 534 # send, list, edit and delete with their permission cases
