<!-- SPDX-License-Identifier: Apache-2.0 -->
# What did not survive verification

98 findings were rejected by the verification pass.
They are recorded so nobody re-files them, and so the shape of the false
positives is visible: most are a reviewer not following the code far enough,
or not knowing that something was deliberate.


## ci-release

- **The two release-please components conflict on the shared manifest, and there is no way to re-run the workflow to heal it**
  The mechanical half checks out (the manifest is four lines, `separate-pull-requests: true` at release-please-config.json:3, and adjacent-line edits on two branches do conflict in git), and release.yml really has no `workflow_dispatch`. But the consequence does not follow, and the finding did not follow the trigger through. release.yml fires on `push: branches: [main]`, so merging the first release PR *is itself* the event that re-runs release-please, and release-please recreates its per-component release branch from the new main HEAD on each run rather than leaving the stale branch in place - the manifest edit is recomputed against a main that already carries the other component's bump, and merges cleanly. The claimed recovery ("an empty or unrelated commit on the default branch") is therefore never needed, and even absent that, re-running the previous release workflow run is a button in the Actions UI. There is also no record anywhere in the repo or CLAUDE.md of this ever having wedged. Adding `workflow_dispatch` may still be nice to have, but it is not a finding at high severity, and the "wedged release state with no in-band recovery" premise is wrong.
- **Two independently floating Rust toolchains build the container image and the downloadable binary**
  The facts are right (rust-toolchain.toml is `channel = "stable"`, docker/server.Dockerfile:16 is `FROM rust:1-alpine`, and the binaries link Ubuntu's musl-tools while the image links Alpine's musl) but the harm is asserted, not evidenced: "a miscompile or a codegen-dependent behaviour difference between them is possible and nothing would report it" is a hypothesis with nothing behind it, and both toolchains track the same channel so they agree in practice within a release cycle. Committing `channel = "stable"` to rust-toolchain.toml is a deliberate choice, not drift, and the secondary complaint (a stable roll can turn `clippy -- -D warnings` red on unchanged main) is the accepted cost of that choice for essentially every Rust project. Filing it would send work at a preference. If reproducible builds become a stated requirement it belongs in a decision record, not a defect list.
- **licenses.yml runs a full Flutter toolchain and pub resolve on every push to main regardless of what changed**
  This argues against an explicitly documented decision. docs/ci.md:128 states the design in the finding's own terms - path-gated "on the manifests, the lockfiles and the policy, plus every push to `main` whatever changed" - and docs/ci.md:57 gives the convention it belongs to: "Every push to `main` runs it whatever changed, so `main` is never trusted on a path filter alone." That covers the case path filters miss (a merge whose combined content differs from what any PR run saw), which is not "nothing". The cost is one extra Flutter runner on docs-only main pushes, i.e. minutes, and the finding's own preferred alternative (an advisories check on a schedule) is already filed separately. Nothing here is broken.

## dart-api

- **(no finding rejected outright)**
  Every one of the 23 findings had verifiable evidence at the cited lines, so none is a measurement artifact. Six were materially wrong in a load-bearing part and are confirmed only in corrected form - listing them here so the corrections are not lost: (1) 'EventConnection.connect ... the reconnect path in SyncController sees an exception type it was not written for' is FALSE - sync_controller.dart:100 is a bare `catch (_)` that treats every failure as offline-and-retry, so only the socket leak survives, at low rather than medium. (2) The ~55 duplicated decode sites are actually ~40 (13 list, 27 map), and the per-file figures 7/6/5 are really 4/4/2. (3) The base-URL-prefix finding was filed as 'a deployment behind a subpath reverse proxy cannot be reached at all', implying a supported configuration is broken; the server mounts every route at root with no strip-prefix anywhere and deploy/Caddyfile proxies whole domains, so subpath hosting is not supported and the defect is the silent drop, medium not high. (4) The 100-id batch cap is a `>=` guard, so the real ceiling is 99, and the one live consumer swallows the resulting error (presence_controller.dart:43) rather than surfacing it. (5) The 413 finding is stronger than filed, not weaker: the equal-threshold DefaultBodyLimit fires before the handler's own 400, so 413 is the live answer for an oversized upload and the handler's `BadRequest("attachment is too large")` is unreachable. (6) The path-encoding finding's extension to the three invite-code routes is a non-issue - invite codes come from an alphabet with no slash (store/invites.rs:86) and a mistyped code already reports correctly as unusable. Also downgraded on severity without rejecting: the `values.byName` finding (the whole fromJson layer is uniformly non-defensive, so the enum sites are not a hole in a complete defence), the dead-code finding, the 'One tag per file' comment, and the NotImplementedException doc typo (which fails loudly at compile time if a caller acts on it).

## dart-packages

- **AppTouchTargets is never mounted in the app, so its InheritedWidget half is dead and only the width fallback runs**
  The grep is right (only touch_targets.dart:22 and touch_targets_test.dart:95 construct one) but nothing is contradicted, because the class doc says the width fallback is the intended primary mechanism, not a degraded path: "Falling back to width rather than to a hardcoded false is what makes this unforgettable: a control is at touch density on a phone because of where it is drawn, not because someone remembered to say so" (touch_targets.dart:33-37). `of()` is used by button, icon_button, list_row, menu and focusable_tap_target, so the class is load-bearing; what has no user is a two-field override escape hatch that is documented, tested, and correct when someone needs it. Recommending it be mounted "so the override path is exercised by something real" would add a widget whose only purpose is to be used.
- **Four of six AppActions have no binding site, so the shortcut table is half decoration**
  The facts hold (only quickSwitch at home_shell.dart:136 and escape at emoji_picker_panel.dart:111 / command_palette.dart:132 are bound) but this is already a recorded known gap, listed verbatim in the CLAUDE.md of the time under "Known gaps left from Phase 2, deliberately" (commit 93e1b645; the section did not survive the PR #666 rewrite): "The shortcut table exists but is not yet bound into the widget tree." Re-filing it sends work at something already on the ledger. The aggravating claim that makes it worse than absent is also false: AppAction.label has no consumer anywhere in packages/app - the only `.label` hit in command_palette_items.dart:145 is PaletteResultItem.label, a different type - so no keybinding screen exists and nothing is presented to a user as working.
- **AppMenuItem alone has no focusNode, and its 'active' local means enabled where AppIconButton.active means toggled-on**
  Both halves fail. `active` at menu.dart:170 is a private local variable inside build, not an API name, so it cannot collide with AppIconButton's public `active` field at any call site - and it is immediately used as `enabled: active`, which is self-documenting where it is read. The missing focusNode rests on a use case that does not exist: command_palette.dart keeps focus in its AppInput (`focusNode: _focusNode`, line 162) and drives selection with a `_highlighted` index passed to AppMenuItem as `selected: at == _highlighted` (line 239-241) under CallbackShortcuts, deliberately never moving focus down the result list. AppMenuItem taking no node is consistent with the one consumer AppMenu's doc names.

## dart-providers

- **Resuming the app re-registers push on every resume when Android notifications are blocked, because the retry check does not use the _isRegisteredWithServer getter**
  The current condition is deliberate and documented; the proposed fix would break the path it claims to protect. push_controller.dart:370-374, immediately above the code cited, says the resume retry exists so that "a denied permission, a token that had not arrived, or a server that was briefly unreachable all self-heal the same way: try again the next time the user opens the app" - a denied permission is explicitly one of the three. That is the only mechanism by which `registeredNotificationsBlocked` can ever become `registered`: `_applyAndroidResult` (:275-287) decides between the two by calling `_requestAndroidPermission()`, so a user who grants notifications in system settings needs a resume to re-ask. Using `!_isRegisteredWithServer` would strand them as blocked for the rest of the launch. The getter's job is the *lifecycle-report* distinction, which is exactly how the adjacent line :384 uses it, and its own doc says so. The two lines are consistent, not divergent.
- **Two providers do 'listen to liveEvents, invalidate membersProvider' with hand-rolled subscription plumbing each time (seven copies)**
  The count conflates two shapes that cannot share the proposed abstraction. Four of the seven (presence_controller.dart:21/50, pins_controller.dart:46/100, typing_controller.dart:31/64, message_extras.dart:60/253) are StateNotifiers holding `late final StreamSubscription<api.ServerEvent> _sub` and cancelling in a `dispose()` override - the idiomatic shape, which no `Provider.autoDispose` factory or ref-based mixin can absorb. Only the two in member_presence.dart:51-75 and :86-93 use `ref.read(...).listen` plus `ref.onDispose`, and one of them owns a debounce Timer it must also cancel, so a shared factory would save roughly two lines across two call sites. Every one of the seven does cancel correctly, so the stated risk ("each with its own chance to forget the cancel") has not materialised anywhere.
- **PinsController and ChannelSearchController repeat the same mounted-guard / Forbidden / ApiException triple, and their state classes are near-identical**
  "Near-identical" does not survive reading them. PinsState (pins_controller.dart:27-36) has three fields and deliberately preserves the previous `pinned` list through a failure so a header pill never blanks; ChannelSearchState (channel_search_controller.dart:24-44) has six (open, query, results, loading, failed, forbidden) and deliberately discards prior results, and its `toggle()` depends on being replaceable wholesale. A shared AsyncOutcome<T> would have to carry both policies plus the search-specific fields, which is more machinery than the 12 lines it replaces. Two instances is also thin ground for an abstraction, and pins_controller.dart:62-66 already documents the parallel on purpose so the guard cannot be dropped by someone who thinks it is incidental.
- **customEmojiImageProvider is a non-autoDispose family whose doc calls a 500 MiB ceiling bounded**
  The comment it objects to states both multiplicands openly - emoji_catalog_provider.dart:47-49 cites the 500-item cap (MAX_CUSTOM_EMOJI) and the 1 MiB per-image cap (emoji.rs:34, verified) - so a reader is given the arithmetic rather than misled by the word "bounded", and the point being made there is the real one: unlike an attachment cache, this set has a ceiling at all. Reaching 500 MiB requires a deployment holding 500 emoji that are each within a rounding error of the 1 MiB limit *and* every one of them rendered in a single session; a realistic custom emoji is tens of KB. No measurement or symptom is offered, and the finding proposes replacing a documented warm cache with an LRU on the strength of a worst case nobody has produced.

## dart-screens

- **Reactions, attachments and polls hydrate only for the first channel opened, because the State is reused across channel switches (their critical)**
  The premise is stale, and the stale doc comment they quoted is what created it. router.dart:144-150 gives every channel its own page: `fadeThroughPage(context, ConversationPane(channelId: channelId), key: ValueKey('channel-$channelId'))`. A distinct page key makes go_router's Navigator remove the old route and push a new one, so `ConversationPane` and `ChannelScreen` are a fresh element subtree per channel, `initState` runs again, and `_hydrateExtras()` re-fetches `listMessages(channelId, limit: 50)` and calls `applyMessages` for every channel opened - which is exactly what message_extras.dart's own file doc describes ('re-fetching the channel's recent messages on open (see [ChannelScreen]) re-hydrates this cache'). page_transitions.dart:25-27 documents the key's purpose in those words. `git blame` puts the 'builds this widget with no key' comment at dca58e6 (2026-07-27) and the per-channel page key at ca1669b (2026-07-29), so the comment simply outlived the routing it described. Everything downstream of the premise - #general then #random rendering blank polls, `key: ValueKey(channelId)` as a fix - does not happen. The comment itself is filed as a confirmed low finding instead.
- **Three admin list screens repeat the same scaffold-plus-async-list-plus-busy-card skeleton verbatim**
  What repeats is a call to a shared component with its arguments, which is the componentised form, not duplication awaiting extraction. `SettingsScreenScaffold` already exists for exactly this and all seven settings/admin screens use it; `AppAsyncView` already owns loading, error, empty and data. The residue is six named arguments (title, backTooltip, backFallback, errorMessage, emptyMessage, separator gap) that differ per screen because they are the screen's content, plus a `ListView.separated` that differs in item type. An `AdminListScreen<T>` taking title, provider, errorMessage, emptyMessage, separator, itemBuilder and actions is seven parameters of indirection to save eight lines of declarative configuration, and would put the type parameter between a reader and the list. The one piece genuinely worth deduplicating is small and specific: `backTooltip: 'Back to Space settings'` with `backFallback: Routes.spaceSettings` at six of the seven call sites could be the default when the caller gives neither. Filed at that scope it would be a trivial cleanup, not a medium componentisation finding, and their own claim that 'the twenty lines above it are boilerplate' does not survive counting them.
- **debug_log_screen passes AppTokens down as a constructor argument while the widget beside it reads Theme.of**
  No defect, and the two supporting claims do not hold. They concede there is no live bug (the parent reads `Theme.of(context)` in its own build, so a theme change rebuilds it and therefore its children). 'Unusable anywhere the caller has not already resolved tokens' is not true of a caller that can write one line to resolve them. And it is not the lone outlier: tokens are threaded as parameters in three other files - presence_menu.dart:135 `_buildMenu(AppTokens tokens, ...)`, command_palette_items.dart:106 and command_palette.dart:201 - so `_EntryTile` is unusual only in taking it through a constructor rather than a method. Threading a resolved theme extension into a list item that is rebuilt in a tight loop is a defensible choice, not a mistake, and there is no project rule against it. Filing it sends work at a preference.

## dart-widgets-messaging

- **The two channel sheets disagree about a valid channel name, and the stricter limit is invented**
  REFUTED at the server. crates/slimm-server/src/http/channels.rs:258-263 `validate_channel_name` returns `BadRequest("name must be 1 to 64 characters")` when `trimmed.chars().count() > 64`, and it is applied on both `create` (line 128) and `update` (line 160). Their grep (`CHANNEL_.*MAX`) only looked for a named constant and missed the inline literal, so the claimed failure scenario - "a channel created with a 100-character name is accepted by both the client and the server" and then permanently unsaveable - cannot happen: the server rejects it first. `_nameMaxChars = 64` matches the server exactly. What remains is a docs nit (the doc comment sits above two constants but names only the topic ceiling, and the server's name ceiling is an inline 64 rather than a citable constant) and a minor asymmetry (create_channel_sheet does no client-side length check, so an over-long name costs a round trip instead of a disabled button). Neither is the filed finding.
- **ManagedChannelRow's comment says "Centring" over CrossAxisAlignment.start**
  REFUTED by history. `git log -S "Centring floats the button"` shows the comment was ADDED in 663d56d, the same commit that introduced `crossAxisAlignment: CrossAxisAlignment.start` and wrapped the kebab in `SizedBox(height: AppListRow.heightFor(context)) > Center`. It is not describing removed behaviour - it is the one-line why for not centring at the Row level (Row-level centring would float the button between a voice row and its participant strip), with the actual centring done inside a row-height box. Terse under the one-line comment cap, but correct and in the right place; "fixing" it back to `.center` is what the comment exists to prevent.
- **Composer failure paths use SnackBars, the exact shape run_guarded.dart was written to remove**
  The code is as quoted (composer.dart:238, :266), but this is not a defect of the composer: `grep -rn showSnackBar packages/app/lib` returns 26 sites across 16 files, including member_actions.dart, presence_menu.dart, avatar_settings_section.dart, join_policy_row.dart and five admin screens. The AppErrorState migration CLAUDE.md records is partial across the whole app, so filing the composer specifically presents an app-wide in-progress migration as a scoped bug and would send work at two of twenty-six call sites. If it is refiled it belongs as one app-wide item with a count. The secondary point - `catch (e)` at :234 with `e` unused, catching Errors as well as exceptions - is real but is defensive breadth around a plugin call with a sensible recovery (refocus and report), not a demonstrated failure, and no lint in packages/app/analysis_options.yaml (flutter_lints only) objects.
- **One of the two recent-emoji writes is unawaited explicitly and the other implicitly**
  Self-declared non-bug, and nothing enforces the convention: packages/app/analysis_options.yaml includes only `package:flutter_lints/flutter.yaml`, which does not enable `unawaited_futures`, and `RecentEmojiController.use` swallows its own write failure either way. A one-word stylistic difference between two call sites with identical behaviour is not worth a reviewer's attention, and filing it dilutes the list it sits in.

## dart-widgets-people

- **Two more private layout breakpoints, against breakpoints.dart's explicit warning about exactly that**
  The premise overstates what breakpoints.dart forbids. Its warning is scoped to one constant: "[kCompactWidth] is shared with the design system, which raises hit targets to touch size at the same width: one pane and a finger are the same situation, and two thresholds for it would drift" (routing/breakpoints.dart:25-28). Neither cited constant is a second opinion about *that* question. `_panelFloor = 900` carries its own reason in a doc comment - "Above this there is room for a 380px panel and a 440px form with gutters; below it, there is not" (onboarding_shell.dart:20-22) - which is arithmetic about a specific composition, not a layout class. `_twoPaneFloor = 800` is the width at which a 240px nav plus a settings pane fit. Their "three different opinions about one window" at 850px does not hold either: onboarding is pre-session and the settings scaffold and the home shell are different routes, so no user ever sees two of these three answers at the same time. This is a preference for centralising constants, not a defect. (The false first sentence of `_twoPaneFloor`'s doc is a real finding and is confirmed separately.)
- **The presence vocabulary is spelled out in four places with three different word sets**
  The four tables are two pairs over two different enums serving two different jobs, and the reasoning given for why it is dangerous is wrong. `presenceWord` (member_profile_sections.dart:131-137) and `_presenceDescription` (member_pane.dart:199-205) are switch *expressions* over `AppPresence` with no wildcard, so Dart requires them to be exhaustive: adding an `AppPresence` value is a compile error in both, which is the opposite of their stated "`AppPresence` has no exhaustiveness check to force it". The one difference between them - `hidden => null` - is documented as deliberate at member_pane.dart:193-198 ("naming it aloud beside their own name tells them nothing they did not choose"). The other pair, `presenceOptions` (presence_menu.dart:26-31) and `PresenceSection._options` (personal_status_sections.dart:25-30), is keyed on `api.PresenceVisibility` - a caller's chosen preference, not a rendered state - and that pair is a genuine verbatim duplicate already filed as the settings-pane copy-paste finding, so confirming it twice would double-count it. The case difference is not accidental either: `presenceDisplayOf` lowercases deliberately, with a doc comment saying why (presence_menu.dart:33-40).
- **Four autoDispose providers are declared inside widget files rather than providers/**
  The premise that "everything else in the app declares providers under providers/" is false, and they undercounted their own case. `grep -rn '^final [a-zA-Z]*Provider' lib/src/widgets lib/src/screens` returns eight, not five: the five they list plus `serverInfoProvider` (channel_rail_frame.dart:26), `voiceSettingsControllerProvider` (screens/voice_settings_screen.dart:85) and `emojiImagePickerProvider` (screens/admin/emoji_upload_card.dart:29). Eight colocated providers across widgets and screens is an established pattern in this codebase, not a drift from a rule, and none of them is documented as an exception because there is no rule to except. The only concrete harm they cite - admin_providers.dart pointing `devicesProvider` and `blocksProvider` at a file that no longer exists - is real and is confirmed separately as the stale-`settings_screen.dart` finding, so nothing is lost by rejecting this one. Moving the providers would be a large mechanical diff justified only by a convention the code does not currently hold.

## docs-accuracy

- **STRATEGY.md records "not a watchtower auto-update from a floating tag" as a decision; the live instance is exactly that**
  The quoted contradiction does not hold as stated, because it conflates two different deployments. STRATEGY.md:457 constrains "the official instance" and explicitly permits the alternative for self-hosters ("self-host docs offer optional opt-in watchtower"). The instance CLAUDE.md describes is not the official instance: that section is headed "Running deployment (LAN test instance)" and the body calls it "A pinned instance ... on the owner's homelab box". No official hosted instance exists yet, and release.yml has no deploy job of any kind (jobs are release-please, server-image, server-image-merge, server-binaries, server-release-assets, linux-client, copr, android-client, ios-testflight), so the decision is unexecuted rather than reversed. The two supporting citations are also weaker than claimed: docs/ci.md:269 and release.yml:149 merely explain what the rolling `latest` tag is for, and deploy/README.md:22 is self-host guidance, which is the case STRATEGY explicitly allows. The only real residue is small and different from the finding: deploy/README.md contains no watchtower guidance at all, so the "self-host docs offer optional opt-in watchtower" half is the unmet clause - not worth a finding on its own.

## dup-client

- **Picker sheets set their own height and are opened without scrolls:true, nesting a ListView inside the sheet's own scroller**
  Both halves fail on inspection. (1) The `SizedBox(height: ...)` they want deleted is documented as deliberate at overwrite_target_picker_sheets.dart:26-29: `/// Bounded height so a loading spinner or an error has somewhere to center; a \`ListView(shrinkWrap: true)\` has no intrinsic height before data arrives.` Deleting it reintroduces the exact problem that comment names. (2) The nested-scrollable claim produces no observable defect. `_SheetDialog` (sheet.dart:90-96) constrains to `maxHeight: MediaQuery.sizeOf(context).height * 0.85` and the picker content is a fixed `0.6 * height`, so the content never exceeds the ceiling and the outer `SingleChildScrollView` therefore never scrolls at all — there is one active scrollable, not two. On the phone branch `scrolls` is not consulted at all (showAppSheet passes `builder` straight to `showModalBottomSheet`), so it cannot matter there either. `ChannelPickerSheet`'s `ListView(shrinkWrap: true)` sizes to content inside the SingleChildScrollView, which is the correct pairing. What is left is that a picker renders a 60%-tall box when a Space has two roles, which is the documented tradeoff, not a finding.
- **Sign-in and onboarding are the only screens using raw Material TextField, because no design-system field carries a label**
  The counts are right (7 labelled `TextField`s across the two screens; `AppInput`'s constructor at input.dart:23-40 has no `label` or `helperText`; there is no AppCheckbox in components/forms) but the stated harm is refuted by app_theme.dart. `buildTheme` purpose-builds `inputDecorationTheme` at :110-135 with the design's own fill (`tokens.surfaceRaised`), `AppRadii.control` borders, a `tokens.focusRing` 2px focused border and a `tokens.dangerBorder` error border, carrying a comment at :131-133 explicitly about labelled-field error grammar: `// A failing field is marked on the field itself (error grammar 03): red hairline, red caption, content preserved.` It likewise themes filledButton, elevatedButton, outlinedButton and textButton at :90-109 with `controlShape` and the design's own `buttonLabel` textStyle. So those screens are not rendering Material chrome outside the design system — the theme is the design system's chosen route for the Material path, and it was written for exactly these fields. The secondary claim that the hand-rolled `Semantics(liveRegion: true, Text(...))` form error is "AppErrorState's exact job" also does not hold: AppErrorState's own doc (error_state.dart:29-30) says "a failure with neither [onRetry nor onDismiss] is usually a failure the caller should not be showing at all", and a form-level error whose retry is the submit button has neither, so it would render as an actionless bordered box.
- **19 lib files are over the 300-line review budget and four test files are past the 500 hard ceiling**
  Every number checks out (voice_screen.dart is exactly 500; 19 lib files over 300, none over 500; 23 test files over 300) but the framing is wrong and the finding adds no information. `scripts/check-file-budget.sh` is a committed CI gate wired into hygiene.yml:268-270 that already emits a `::warning` for every one of the 19 on every run. And the four test files "past the 500 hard ceiling" are not breaching anything: all four are registered in `scripts/file-budget-allow.txt` at exactly the counts measured — push_controller_test.dart 925, new_routes_test.dart 665, session_persistence_test.dart 588, message_row_test.dart 509 — and the gate treats each listed number as that file's own ceiling, failing if it rises. The allowlist header states the intent ("this is a debt register rather than an exemption... Nothing here is a decision that the file is fine"), so the debt is recorded, gated and ratcheted. Two useful facts survive and are worth carrying into other work rather than filing as a finding: voice_screen.dart at exactly 500 means the next line added to it fails CI, so it must be split before it is touched; and CLAUDE.md's note that sync_controller.dart, member_pane.dart and channel_screen.dart need splitting is stale in two of three (they measure 285, 255 and 374, not 316, 396 and 583).

## dup-server

- **The batched/per-user permission equivalence is asserted only for VIEW_CHANNEL, which is structurally the one bit that cannot detect the timeout divergence**
  Refuted: the guard exists in a file the auditor did not open. `crates/slimm-server/tests/member_timeout.rs` contains two tests that cover exactly the two claimed gaps. `the_batched_timeout_lookup_matches_the_single_one` (line 233) compares `timed_out_among_until` against `timed_out_until` per id, including a user with an already-elapsed timeout and the empty-candidate case — that is a direct assertion on the very divergence the finding calls 'structurally invisible'. And `the_batched_paths_agree_with_the_per_user_path` (line 140) sets a real timeout, then drives `viewers_among` and `visible_channels` against `has_permission` across both a text channel and a DM. Its doc comment states the auditor's own observation as deliberate: 'Both only ask about VIEW_CHANNEL today, which a timeout deliberately does not touch, so the outcome here is "nothing changes" - and that is exactly the point: a timed-out member must keep receiving pushes and keep seeing their rail.' The narrow factual claim ('there is no timeout anywhere in tests/permissions.rs') is true but does not support the conclusion, because equivalence for the timeout rule is pinned elsewhere. `a_timeout_reaches_direct_messages` (line 113) and `an_administrator_is_masked_like_anyone_else` (line 262) close the remaining dimensions.
- **Two competing conventions for mapping store errors to ApiError, split eight to thirteen, with the same Internal arm repeated in every one**
  The counts are accurate but the harm is not. The split is not arbitrary: I grepped every one of the error types and the four store types with `From` impls in error.rs (`HashError`, `SendError`, `PushError`, `OpenError`) appear nowhere in src/http outside error.rs — their handlers use bare `?` and never name the type, which is precisely why they need the impl. The twelve matched inline are all matched in the one module that owns their route, and every non-Internal variant produces a message phrased for that route ('the @everyone role cannot be deleted', 'this would leave the deployment with no administrator', 'that search query is not valid'). So the working rule is 'a From impl when the mapping is route-independent, an inline match when the refusal needs route-specific phrasing', which is coherent rather than accidental. The two concrete harms both fail: 'thirteen chances to map an internal error to something other than a 500' is theoretical, since every one of those arms is literally `e.into()` reaching the same `From<anyhow::Error>`; and 'half the store's failures lose their context in the log' is overstated, because the store attaches `.context(...)` to the anyhow errors it wraps (e.g. store/pins.rs's 'loading a message to pin it'), which `tracing::error!(error = %err)` prints. Nothing here is a defect, and mechanically collapsing twelve inline matches would move route-specific wording away from the route that needs it.
- **Seventeen source files are over the 300-line review budget; the two past the 500 hard limit are registered but their named seams are still uncut**
  Every number checks out exactly (17 src files over 300 with the cited counts, 29 test files over 300, six over 500) — but no rule is violated and the finding adds no information CI does not already print. `scripts/check-file-budget.sh:59-63` emits a `::warning` for every file over 300 on every run, by design, and docs/ci.md records why 300 warns rather than fails. The only two files past 500 are both registered in `scripts/file-budget-allow.txt` as accepted debt, which is what that register is for; the real defect in that register is the stale ceiling, already filed separately. The headline remediation is also wrong: 'move push.rs's tests to tests/ and the file is instantly compliant' would move a `#[cfg(test)] mod tests { use super::*; }` block (push.rs:466-618) that exercises private items such as `is_private_host` and the debounce internals into an integration-test crate that can only see the public API. A generic 'these files are long' list plus a speculative split plan for ten of them is a refactor backlog, not an audit finding, and its two genuinely actionable items (sessions.rs's ceiling, store/messages.rs's duplicated projections) are already covered by other findings.

## perf-client

- **ScreenShareView calls setState on every room event, including audio-level churn**
  The listener and the per-event setState are real (screen_share_view.dart:47-51), but the claimed harm is not. `VideoTrackRenderer.didUpdateWidget` re-attaches only `if (widget.track != oldWidget.track)` (livekit_client 2.8.1 widgets/video_track_renderer.dart:196-206), so a rebuild with the same resolved track touches neither the texture nor the view registration. What each event actually costs is one leaf widget rebuild plus `_shareTrack`'s scan over remote participants' video publications - a handful of comparisons. The file's own justification ("a rebuild is cheap next to decoding video") is accurate, and nothing measured contradicts it.
- **Six StreamBuilders construct their drift stream inside build()**
  The stated mechanism is wrong, and the corrected mechanism is a pattern drift explicitly supports. `SimpleSelectStatement.watch()` returns `database.createStream(fetcher).asyncMapPerSubscription(_mapResponse)` (drift 2.31 select.dart:76), and `asyncMapPerSubscription` builds a fresh `Stream.multi` per call (utils/async_map.dart:18-43), so the stream object is never equal across builds and StreamBuilder does resubscribe - the `_ControllerStream` equality the finding rests on is not what is happening. That resubscribe is cheap by design: `StreamQueryStore.markAsClosed` defers removal through `Timer.run` so a listener reattaching in the same turn keeps the cached stream, and the comment there names the reason - "after you stopped listening to a stream, it can keep its cache just a bit longer ... especially with Flutter's StreamBuilder" (stream_queries.dart:131-166). `_onListenOrResume` then pushes `_lastData` rather than re-querying (260-299). So there is no SELECT per build and no resubscribe loop; the worry the finding is built on does not exist.
- **MessageTranscript mutates an unbounded Set inside build, once per message per rebuild**
  The code is as quoted (message_transcript.dart:119-123), but the cost case does not hold. `_seen` only ever receives ids from the current `messages` list, which `watchChannel` caps at 200, so it is bounded by the rows in the window, not unbounded, and the per-build work is at most 200 hash inserts - microseconds. The rebuild frequency used to justify it ("~50 per channel open") is a measurement artifact: 50 `_set` calls in one synchronous loop mark the consumer dirty once and Flutter coalesces to a single rebuild. Writing State from build is a fair style objection, but the write is idempotent and the comment above it already states the intent, so there is no defect to send work at.
- **channel_screen rebuilds two derived collections from scratch on every build**
  Both allocations are real (channel_screen.dart:249-266) but neither has a demonstrated cost: the members list is capped at 200, so this is 200 `toLowerCase` calls and two small set builds per build of one screen, which is microseconds against a 16.6ms frame. The stronger part of the argument - that a fresh Set identity would defeat downstream memoization - is conditional on memoization that does not exist anywhere in the message-rendering path, so it is a prerequisite for a different fix rather than a finding on its own. Filing it would send work at a micro-allocation.
- **Avatar, profile and attachment bytes are autoDispose, so scrolling the transcript refetches them over the network**
  Rejected as filed, because the avatar and attachment halves are documented deliberate decisions the finding did not read. `avatar_bytes.dart`'s library doc says exactly this: "In-memory only and `autoDispose`, the same tradeoff `attachment_bytes.dart` makes, so a long member list evicts what nobody is looking at rather than growing forever" (avatar_bytes.dart:1-8). So the claimed inconsistency with `customEmojiImageProvider` is not one - both files reason explicitly and reach different conclusions for different stated reasons (a bounded 500-emoji set versus an unbounded roster). The one part that does survive is narrower and is filed separately: `userProfileProvider`'s doc comment claims once-per-session caching that autoDispose does not provide.

## perf-database

- **channels.kind has no CHECK, and DM access control - which deliberately has no ADMINISTRATOR bypass - dispatches on its exact string**
  The quotes are accurate but the finding does not survive on either the deliberateness test or the threat model. Migration 0012:3-5 does not merely lack a CHECK, it explicitly considers and declines one in the same sentence the finding quotes ("no CHECK constraint enforces the enum, exactly like 'text'/'voice' already are not enforced"), so this is a recorded decision rather than an omission. And the only user-reachable write path already validates the enum in code: `src/http/channels.rs:129-131` is `let kind = req.kind.as_deref().unwrap_or("text"); if !matches!(kind, "text" | "voice") { return Err(ApiError::BadRequest(...)) }`, and a DM channel row is only ever created by `open_dm` with the `DM_CHANNEL_KIND` constant. That leaves the attacker as somebody with direct write access to the database file - who can read every DM's plaintext straight out of `messages.content` (the finding's own sibling item confirms content is stored unencrypted), or grant themselves ADMINISTRATOR, without touching `channels.kind` at all. The CHECK buys no security against that adversary, so filing it as a security finding sends work at a threat that is not there. At most it is a cleanup nit that 0012 already declined on purpose.
- **ANALYZE never runs, so sqlite_stat1 never exists and every plan but the canvas one is chosen from planner defaults**
  Every fact checks out (`src/db.rs:19-46` sets journal mode, synchronous, busy_timeout and foreign_keys then migrates and stops; `SELECT name FROM sqlite_master WHERE name='sqlite_stat1'` returns nothing on my migrated database) but this names a condition, not a defect, and no misplanned query is offered as evidence of harm. I ran EXPLAIN over the queries the finding lists as at risk and every one plans through the index you would want: the FTS join seeks by rowid, the `list_dm_conversations` OR resolves as `MULTI-INDEX OR` over `dm_channels_pair` and `dm_channels_user_b`, `users_with_push_devices` seeks `sessions_device` per candidate. The one case where defaults genuinely misplanned was measured, and the project chose the durable structural answer over ANALYZE deliberately: `docs/research/canvas-spike-server.md:119-123` records that running ANALYZE made the planner pick correctly and rejects relying on it ("it works on the developer's database and not on anyone's deployment"), pinning the plan with `CROSS JOIN` plus `tests/canvas_index.rs` instead. Enabling `PRAGMA optimize` would move plans across the whole schema with nothing measured motivating it and would require rewriting the one plan test that exists. That is a change with real downside risk proposed against zero observed harm.
- **server_meta is the only non-STRICT table, and it holds a live setting**
  The observation is correct - querying `sqlite_master` for tables whose SQL lacks STRICT, excluding FTS and R-Tree shadow tables, returns exactly `server_meta` (`0001_init.sql:6-9`) - but the stated failure mode does not occur, and it is the whole justification. The finding says "a hand-edit that puts an integer where the reader expects text is unchecked here". `value TEXT` gives the column TEXT affinity, which in a non-STRICT table coerces a numeric value on the way in. I ran it: `UPDATE server_meta SET value = 42` then `SELECT typeof(value), value` returns `text|42`, so `Store::deployment_name`'s `value AS "value!"` still decodes fine. Only a BLOB escapes affinity conversion, which is not a plausible hand-edit. Separately, the framing that this table drifted into being a settings table is not right either: `0010_server_identity.sql:18-20` deliberately reuses it and says why ("server_meta already exists as a generic singleton settings table (migration 0001), so a new key here is a real, persisted field rather than a value invented in application code"), and `src/store.rs:227-234` repeats that reasoning. What is left is a cosmetic consistency point whose cost is a rebuild of a two-row table.

## perf-server

- **No ANALYZE is ever run, and the documented "single serialized writer plus a read pool" is actually one 8-connection pool**
  Both halves fail. The ANALYZE half names no query that is mis-planned today. The single case the project was ever bitten by is the canvas viewport, and store/canvas.rs already pins it with `CROSS JOIN` plus a plan assertion in tests/canvas_index.rs - which is what makes it safe *without* ANALYZE, and running ANALYZE in production but not in the test would create a divergence between the pinned plan and the live one. Proposing `PRAGMA optimize` on the strength of "every multi-index query is planned against empty sqlite_stat1" is speculative optimization with no measurement behind it, which is exactly what this project's own notes say not to file. The pool half misreads a deliberate, documented design: `Store::begin_write`'s doc comment (store.rs:165-178) states outright that "The pool hands out eight connections and any of them may write, so two requests really can be inside a transaction at once" and explains that `BEGIN IMMEDIATE` is the answer *because* a deferred BEGIN cannot wait on busy_timeout while an immediate one can. So the claim that "contention surfaces as SQLITE_BUSY after the 5 s busy_timeout rather than as in-process queueing" is backwards - BEGIN IMMEDIATE does honour busy_timeout, which is the whole point of that helper existing. What is left is that the CLAUDE.md of the time carried a one-line architecture summary describing a writer/reader split the code does not have. That is a two-word correction to a prose knowledge-base entry, not a perf-server finding, and it should be fixed by editing the sentence rather than by building a second pool nobody has shown a need for.
- **store/sessions.rs is 809 lines, past the 500-line hard ceiling**
  Filed as if it were an unnoticed rule violation; it is a registered, CI-enforced, already-shrinking debt entry. `scripts/file-budget-allow.txt` lists `crates/slimm-server/src/store/sessions.rs 957 # tokens, refresh rotation, ws tickets and account deletion in one file; account deletion is the obvious first split`, and scripts/check-file-budget.sh (run by the hygiene workflow's "file size budget" step) treats that number as the file's own ceiling and fails if it is exceeded. The register's header says explicitly "Nothing here is a decision that the file is fine... this is a debt register rather than an exemption." So the split is already recorded, with the first seam already named, and at 809 lines the file has come down 148 lines from where it was registered - it is moving the right way under an active ratchet. There is nothing here a reader does not already have, and the finding is also outside the perf-server scope.

## rust-http-core

- **The rate limiter never sees a request that fails authentication, and there is no class for reads at all**
  The mechanical facts check out (Authed runs at extraction, `enforce` in the body; no read class in ratelimit.rs:25-53; `/version` does two DB reads unauthenticated), but the finding's own stated harm is refuted twice by the code it cites. First, the guarantee it says is broken is upheld: ratelimit.rs:4-6 promises that one client cannot keep the Argon2id permits saturated, and register/login declare `_limited: RateLimited<PASSWORD>` as their *first* extractor (auth.rs:107, 164), so the hash-bearing endpoints are throttled before any work - that is the only claim the module doc makes. Second, per-IP flooding by an unauthenticated caller is an explicitly recorded non-goal, not an oversight: extract.rs:52-57 documents why the unauthenticated key is the peer address and states "A reverse proxy that terminates for real clients should apply its own per-IP limit as well." What is left is that a bearer-token miss costs one SHA-256 plus one indexed SELECT - the same order as /healthz's `ping`, which no in-process limiter can remove. The genuinely actionable residue (no class covering /sync and the search route) is already inside the first finding, and filing it twice would send the same work at the same problem under two names.
- **Sixteen hand-rolled body-limit constants and a pagination idiom repeated over four separate constant pairs**
  Counts are close but wrong (18 body-limit constants, not 16), and more importantly there is no defect here. The values genuinely differ across the surface - 1024, 4K, 8K, 64K, 2M - because each router's ceiling is a per-route decision, and users.rs:46-52 shows that being made deliberately, splitting one module into two sub-routers so the avatar ceiling does not leak onto the JSON routes. Fourteen private constants that happen to share two values is locality, not duplication: centralising them would couple every module to a shared header for a number each one is entitled to choose, and the finding names no failure that follows from the status quo (its "the fifteenth is free to differ silently" is the intended property). The pagination pairs are likewise different numbers for different resources (canvas 500/2000, members 50/200). One fragment does survive and is too small to file: search.rs:31 says its page size is "matching plain message history" while restating messages.rs:36-37's values, so that comment asserts a coupling it cannot enforce - a one-line doc fix, not a finding.
- **sync validates a cursor less strictly than the read-marker route beside it, and aborts a batch on one bad id**
  The two observations are accurate (sync.rs:163 propagates a parse failure as a 400 while an unviewable scope is skipped at 166-172; `after_seq` takes a negative where put_read refuses one at 117-119) but neither is a defect, and the finding concedes the second is harmless. The asymmetry it treats as inconsistency has a documented reason that is not leniency: the skip exists so "sync never confirms a hidden channel exists" (sync.rs:164-165), an information-hiding rule that says nothing about malformed input. Refusing a malformed body with a 400 is what every other `parse_uuid` site in the crate does and is the standard answer; switching to a silent skip would hide a client bug rather than surface it. The harm narrative - a client with one corrupt local channel id losing catch-up for 199 others - is speculative: channel ids reach the client from the server, and no evidence of such corruption was offered or found. At most this is a request for `BadRequestDetail` to name the offending id, which is a nicety, not the correctness problem it is filed as.

## rust-http-rest

- **emoji.rs's DTOs are pub inside a private module, alone among its siblings**
  The observation is factually correct - I verified emoji.rs:52-63 declares `pub struct CustomEmojiDto` and `pub struct UploadParams` with pub fields, that http.rs:27 declares `mod emoji;` privately (only `capability` is `pub mod`), and that grepping crates/ finds no reference to either type outside emoji.rs. But it names no defect, no behaviour, and no project rule: there is no `unreachable_pub` lint in play (crates/slimm-server/Cargo.toml:60 sets only `[lints.clippy] all = warn`), `#[derive(Debug)]` on a DTO is unremarkable, and CLAUDE.md's conventions say nothing about item visibility. Filing it spends reviewer attention on a `pub` keyword. If it gets fixed it should ride along with another change to emoji.rs, not be tracked.
- **An invite can be created already expired (the second half of "A poll can be created already closed, and an invite already expired")**
  Split off and rejected; the poll half is confirmed separately. The invite half is not a defect: `invites::create` returns the full `InviteDto` through `dto(invite, now_ms())` (invites.rs:53-56), and `usable` is computed by `Invite::is_usable` (store/invites.rs:37-42), which already requires `expiry > now`. So a caller passing a past `expires_at` gets a 200 whose body says `usable: false` in the same response - the answer is self-describing, unlike the poll case where the side effect is a permanent transcript entry. Their own direction concedes this ("arguably self-describing, so either refuse it or say in the handler doc"), which means it is a documentation preference rather than a finding.
- **Three mutating handlers charge no rate limit while every sibling verb charges Class::Write (the framing, not the substance)**
  The finding survives and is confirmed above, but two load-bearing claims in it are wrong and must not be carried forward. "Every sibling verb charges Class::Write" is false: `auth.rs:222 logout`, `auth.rs:235 delete_account` and `sync.rs:110 put_read` charge nothing either, so six mutating routes are uncharged, not three. And `remove_device` is the weakest of their three rather than a standout - it sits alongside `logout` and `delete_account` in self-service session management, none of which charge, so it is consistent with its actual neighbours. The route they did not file, `PUT /channels/{id}/read`, is the one an ordinary member can abuse without any privileged bit; ranking `remove_device` above it would aim the fix at the least exposed of the six.

## rust-modules

- **media.rs and voice/mod.rs are both over the 300-line review budget with a clean, purely additive seam in each**
  The line counts are right (media.rs 349, voice/mod.rs 374) but singling these two out is a measurement artifact of what the reviewer happened to be reading. `git ls-files '*.rs' | xargs wc -l` shows 30+ Rust files over 300, including larger non-test source files the finding does not mention: store/sessions.rs 809, http/messages.rs 492, http/ws.rs 469, store/messages.rs 452, store/polls.rs 444, http/users.rs 393. scripts/check-file-budget.sh warns rather than fails above 300 by design (docs/ci.md), and the CI run already emits that warning for every one of those files, so nothing here is unreported. Filing the 27th and 31st largest files as findings sends work at an arbitrary slice of a repo-wide, already-visible condition.
- **spawn_token_sweep and spawn_attachment_sweep are the same detached-ticker loop written twice**
  The shared part is three lines - `tokio::spawn`, `interval(...)`, one throwaway `tick().await` - and everything after diverges: the token sweep logs three named counters off `SweptTokens` (lib.rs:90-97), the attachment sweep iterates the returned hex ids and deletes each backing file with its own per-file warning (lib.rs:123-129). A generic `spawn_sweep<F, Fut>` over three lines, with both bodies handed in as closures returning futures, is more machinery than it removes, and lib.rs:112-113 already cross-references the shared model in prose so the reasoning is discoverable. No defect and no clear improvement.
- **VoiceService::sign and admin_token take &self and never use it**
  Factually right (voice/mod.rs:300-317 touches only `enabled` and `claims`; :272-293 only `enabled` before delegating), but there is no failure mode: both are private methods with three call sites in the same module family, every one of which passes `self.inner.as_ref()`'s own `Enabled`, so the 'a caller could pass a different Enabled' concern is not reachable from anywhere. It is a clippy-pedantic style preference with no behavioural consequence. Worth folding into the twirp-helper refactor above if somebody is in the file anyway, which is why it is mentioned there rather than filed on its own.
- **The attachment size default is a magic number in two places, acknowledged by a comment rather than shared**
  The duplication is real (media.rs:32 against config.rs:90-92) but the constant is test-only: `DEFAULT_ATTACHMENT_MAX_BYTES` has exactly one use, `Media::for_tests` (media.rs:207), and production always passes the config value (lib.rs:42 `media::Media::new(config.attachments_dir.clone(), config.attachment_max_bytes)`). Divergence would change what a test fixture accepts and nothing a deployment does, so the stated failure mode ('tests accepting an upload size production refuses') is confined to the fixture and has no production path at all. Too small to spend a change on.
- **RateLimiter::tracked() is pub but only its own tests call it, where the equivalent typing accessor is #[cfg(test)]**
  Both halves check out (`tracked()` is called only at ratelimit.rs:232 and :237; typing.rs:94-97 gates `is_typing` behind `#[cfg(test)]`), but this is a doc-wording and visibility preference with no failure mode: an unused `pub` accessor on a crate-internal type cannot be misused by anything, and the 'and diagnostics' phrase is aspirational rather than wrong. Not worth a finding.
- **PushKind carries two independent sources for the same wire string**
  The code says the opposite of the finding. push/envelope.rs:38-39's doc comment on `wire_str` is explicit that this is deliberate: 'The relay's own `kind` field, a separate string from this envelope's serialized form (which lives inside the sealed, opaque payload).' They are two different protocol fields on two different surfaces - one inside the sealed box the relay cannot read, one in the relay's own routing request - that happen to carry the same value today. And it is not untested: tests/push_relay_contract_fixture.rs:258 asserts `entry["kind"] == "message"` on the real emitted request, :354 says a changed kind fails there in server CI, and :385-386 drives an unknown-kind case through the relay's real handler. Coupling the two would tie the envelope's internal vocabulary to the relay's, which is the thing the doc comment declines to do.
- **Event derives Debug while carrying full message content, against the no-Debug-on-sensitive-payloads rule the neighbouring module states explicitly**
  Removing `Debug` from `Event` would not achieve what the finding claims, because `Message` itself derives it: store.rs:119 is `#[derive(Debug, Clone)] pub struct Message` with `pub content: String`, and it is returned from the store into every http handler. So `tracing::debug!(?message)` remains one keystroke away in http/messages.rs whatever `Event` derives, and the finding's whole premise (that Event is the easiest thing to log) does not survive that. The precedent it cites is also a different rule: push/relay.rs:24-25 and :39-41 withhold `Debug` from bearer tokens and sealed payloads - credentials - whereas message content is plaintext this server holds by design and returns in ordinary HTTP responses. The finding itself notes no call site logs an Event today, so nothing is wrong now either.
- **The Argon2 decoy password is a bare literal written twice**
  The two literals are there (auth.rs:71 and :133) but divergence has no consequence whatsoever, which the finding half-concedes and then files anyway. `verify_decoy` (auth.rs:131-137) discards the boolean with `.map(|_| ())`, and an Argon2id verification costs the same whether the password matches or not - the entire purpose is to burn one verification's worth of time on the no-such-user path. A mismatched literal would still acquire the same permit, still run the same hash on the same blocking pool, still return `Ok(())`, and still close the timing oracle. A style nit with a stated failure mode that does not fail.

## rust-store-core

- **`visible_channels` has no DM branch and nothing pins the SQL filter it silently depends on**
  Both halves are wrong. The dependency is not undocumented: `list_channels` lives in store/bootstrap.rs:123-131 and its doc says the exclusion is deliberate and deliberately central - "Excludes `dm`-kind channels ... Filtering it out here, rather than trusting every caller of this method to do it themselves, is what keeps a DM out of the ordinary channel list even though it lives in the same table." store/channels.rs:130-135 cites that same choice by name for `update_channel`. So the finding's suggested fix (filter in `visible_channels` too) contradicts a written decision. And it is pinned end to end: tests/dms.rs:318 `dm_channel_is_excluded_from_the_ordinary_channel_list` opens a DM and asserts it is absent from `GET /channels`, which is served by `visible_channels` (http/channels.rs:101), for the first-registered account - i.e. the deployment-claiming administrator, which is exactly the ADMINISTRATOR case the finding says cannot be covered. tests/dms.rs:500-548 additionally covers the two write paths that share the table. Nothing here is unguarded or unpinned.
- **The four-arm optional-field UPDATE is duplicated, and its `(None, None)` arm is unreachable from the only caller in both copies**
  The facts are right and the conclusion is not. It is two sites, not a pattern (store/roles.rs:108-147 and store/channels.rs:142-181), over different tables. The `(None, None)` arms are not undeclared dead-defensive code: channels.rs:125-129 says precisely "Callers are expected to reject the case where both are `None` before reaching here ... this still resolves it safely as a plain existence check rather than assuming it can't happen", so the request the finding makes of them is already satisfied. Most importantly the proposed fix - one `QueryBuilder`-based helper - trades away sqlx's compile-time query verification: both functions use `query!`, and this crate depends on that checking through the committed `.sqlx` cache and `SQLX_OFFLINE=true` builds in CI and the Dockerfile. Losing verification on two UPDATE statements to save ~40 lines of mechanical match arms is a worse trade than the duplication, and the finding does not mention the trade at all.
- **`roles_for_users` groups by linear scan and returns a Vec where the two sibling batch paths use a HashMap**
  The code is as described (store/roles.rs:249-257) but there is no defect. The finding concedes the cost is bounded by the 200-member page cap and "not a real cost today" - at that bound this is a few tens of thousands of 16-byte comparisons, microseconds, off any hot path. The recommended shape is also wrong for this call site: the query ends `ORDER BY mr.user_id, r.position DESC, r.created_at` and the Vec preserves that role ordering, which is what the badge rendering in http/users.rs::to_dtos consumes; a `HashMap` return would discard it, and the finding's own parenthetical admits ordering probably matters. If anything wants changing it is `iter_mut().find` to a check of `last_mut()` (rows for one user are contiguous by the ORDER BY), which is not worth a finding.
- **Five batched paths bind one SQL parameter per element with no chunking or documented ceiling**
  Contains a factual error and the residue is not actionable. "Only `roles_for_users` has a caller-side cap" is wrong: `user_profiles` is capped at `MAX_USER_BATCH = 100` in http/users.rs:41 and enforced at :256 before the store is called, and `timed_out_among_until`'s HTTP caller is http/users.rs:121 inside `to_dtos`, which is only ever handed the paged member list or that same capped batch. That leaves two operator-scale lists (`viewers_among` from `users_with_push_devices`, `visible_channels` from live channels), and reaching SQLite's 32,766-variable limit needs ~32k push-registered devices or ~32k channels on a product whose stated target is small self-hosted friend groups. The finding itself says "this is a robustness note, not a live bug". Chunking two builders and merging results to guard against that is work sent at a problem that does not exist.

## rust-store-rest

- **Moving a canvas object does not advance its seq, so a cursor-based viewport read can never report the move**
  The code observation is accurate (canvas.rs:203-215 writes x/y/w/h with no seq allocation, unlike place_canvas_object) but the stated consequence cannot occur. There is no canvas write route: http/canvas.rs:41-42 mounts exactly one route, `get(viewport)`, and its own comment at line 156 says the read-after-write concern is "the whole point once a write route exists (Phase 6)". `move_canvas_object` has no caller outside crates/slimm-server/tests/canvas_index.rs, so no client can move an object and no peer can render a stale position. The root cause is also already recorded with its designed answer: docs/research/canvas-spike-server.md:214-218 states that a mutation not advancing seq means "no cursor over `canvas_objects` can ever observe one", that the answer is the `canvas_ops` stream, and that patching a second ordering authority into the object cursor is deliberately refused - and lines 205-212 already say live changes reach clients by WebSocket fan-out, not by the pull cursor. This is a Phase 6 design item already on the record, not a defect in shipped behaviour.
- **update_channel writes the same UPDATE four times to vary two optional columns**
  The duplication is real as described (channels.rs:141-181, four match arms, the `deleted_at IS NULL AND kind != 'dm'` guard four times) but the recommended fix is a net loss and the stated risk is thin. Every fixed-shape query in this crate uses `sqlx::query!`, which type-checks against the committed `.sqlx` cache that CI and the Docker build depend on (`SQLX_OFFLINE=true`); the QueryBuilder rewrite would trade that verification for line count. QueryBuilder is used in this directory only where a macro cannot express the shape at all - variable-length id lists (users.rs, reactions.rs, attachments.rs, push.rs, polls.rs) - which is a different situation. And the "four chances to be missed" claim does not hold up: all four guards sit inside one 40-line match a reader sees whole, unlike the nine-site projection duplication, which is filed separately and confirmed.

## sec-client

- **The local message cache is only wiped on a session-end edge, so the fresh-install branch that clears the key store leaves the previous account's messages readable**
  The code reading is accurate - providers.dart:167-171 clears the key store and returns without touching drift, and store.clear() is reachable only from sync_controller.dart:265 on a 1-to-0 session edge - but the stated exposure cannot arise on any platform this ships to, so it is a scenario artifact rather than a finding. The branch fires exactly when SharedPreferences lacks hasLaunchedBeforeKey while secrets survive, and providers.dart:161-165 names the only situation that produces that: a keychain outliving app deletion on iOS/Android, where the app container - and with it slimm.sqlite - is gone anyway, so there is no cache left to read. Their claim that 'on desktop and web the cache outlives whatever wiped the preferences' does not hold on the desktop target: openSlimmDatabase uses getApplicationSupportDirectory() (client/packages/data/lib/src/connection/native.dart:22), FileKeyStore writes slimm_secrets.json into the same directory (client/packages/platform/lib/src/persistent_key_store.dart:125-126), and shared_preferences_linux writes its json there too (~/.pub-cache/hosted/pub.dev/shared_preferences_linux-2.4.1/lib/shared_preferences_linux.dart:339-343 `final String? directory = await pathProvider.getApplicationSupportPath();`). All three artifacts share one directory, so nothing removes the preferences without removing the database and the key store as well, and the branch never fires with a stale cache present. The secondary note (no secure_delete or VACUUM after clear()) carries no evidenced defect; if it is worth doing it should be filed on its own terms rather than as support for a scenario that does not occur.

## sec-input-dos

- **Sub-claim: POST /sync can drive roughly 2000 sequential queries from one request**
  Arithmetic is right per scope (~10 queries) but the 200x multiplier is not reachable without 200 distinct channels the caller holds VIEW_CHANNEL on, which a small self-host does not have. With nonexistent channel ids the cost is 2 queries per scope (`granted_in_channel` returns early at `channel()`, `timeout_deny` still runs as the argument to `.remove()`), so the floor any member can reach is ~400, not ~2000. The finding survives with corrected arithmetic; the 2000 figure should not be quoted forward.
- **Sub-claim: the Upload rate limit 'cannot protect the resource it exists to protect'**
  `ratelimit.rs:48` states the resource Class::Upload protects is disk, and disk is protected: `media.write_attachment` at attachments.rs:118 runs after both `enforce` and the ATTACH_FILES check, and `attachments::routes(max_attachment_bytes)` applies `DefaultBodyLimit::max(...)` so the buffered body is bounded at the operator's configured ceiling. What the ordering actually exposes is memory and bandwidth for a refused caller, which is a narrower claim. Folded into the confirmed HTTP-bounds finding with corrected wording.
- **Sub-claim: viewers_among's DM N+1 is 'on the message send path'**
  It is not. `PushSender::notify_message` (push.rs:148-166) hands the recipient lookup to a detached `tokio::spawn(deliver(...))` and its doc comment says so: 'Synchronous and cheap ... the recipient lookup, lifecycle gating, per-recipient debounce, sealing, and relay call all happen in a detached background task, so this never blocks or fails the caller.' The N+1 is real and the two comments describing it are wrong, but it does not add latency to a send, which is why the finding is confirmed at low rather than medium.
- **Sub-claim: pins::routes() sets no DefaultBodyLimit, unlike every other route group**
  True as stated but not a defect. axum applies a 2 MB default body limit when no `DefaultBodyLimit` layer is present, and none of the three pin routes takes a request body at all (`put`/`delete` on the pin path and two `get`s). Dropped from the pins finding so it does not read as a missing bound.

## sec-server-authz

- **A role-granting invite can be redeemed by an existing member, so sharing an invite link escalates anyone already inside**
  The mechanism is real (redeem at http/invites.rs:242-257 requires only authentication, and store/invites.rs:216-238 applies `role_grant` to whoever asked), but it is not a distinct authorization flaw and the framing does not survive. A role-granting invite is a bearer credential whose documented meaning is the role: tests/invites.rs:615-618 states it as "an invite's role is who redeeming it becomes", and store/invites.rs's own doc on `spend_invite` names "redemption by an existing account (Store::redeem_invite)" as one of the two intended paths sharing that statement. More importantly the proposed fix closes nothing: anyone holding a leaked code who is refused here can spend the identical code on POST /auth/register instead and receive the same role (tests/invites.rs:490-535 pins exactly that path), so the role reaches the unintended holder either way and the only thing gained by restricting redemption to new accounts is that they need a second account. The residual concern is not authorization but that `max_uses` defaults to unlimited (http/invites.rs:67-68) for a code that carries a role - a product default worth a look, not the reported bug.

## tests-quality

- **Every snapshot renders with `permissions: -1`, so no permission-gated hide branch is in the overflow gate**
  The evidence is right (`'permissions': -1` in fixtureClient's /me answer, and all 11 surfaces x 2 themes go through it) but the consequence does not follow. The overflow assertion is the only machine check in that harness, and hiding a section makes a screen shorter - the all-permissions render is the worst case for overflow, so a member variant would gain the machine gate nothing. The structural behaviour they worry about is already pinned by real widget tests: settings_space_section_test.dart drives the gating bitmask directly and asserts the section and its 'Channel permissions' row are absent at lines 30, 43, 54, 83 and present at 64 and 106, including the ADMINISTRATOR-bypass case documented at line 95. And the PNG half is not diffed in CI (SLIMM_UI_SNAPSHOTS only), so the added renders would exist for a human to look at, which is a nice-to-have rather than a finding.
- **192 multi-line `///` comments inside test function bodies, which the comment-cap gate exempts without checking they document an item**
  The count is off by roughly 5x and the count is what the finding rests on. Their examples are real - golden_matrix_test.dart:205-207 and message_row_test.dart:431-433 are multi-line `///` blocks above a statement - but scanning every client test/harness file for runs of 2+ `///` lines whose next non-comment line is a statement finds 37 runs and 126 lines, not 192, and several of those 37 are false hits on legitimate top-level declarations (composer_harness.dart:106 documents `const _tokens`, emoji_admin_screen_test.dart:33 documents `const _png`). The likely source of 192 is counting `///` lines at brace depth > 0, which sweeps in every doc comment on a member inside a class body - a correct use. On the real number (~30 sites), and with CLAUDE.md and check-comment-cap.sh both stating the doc-comment exemption deliberately and warning in the same breath that it is "not a loophole", this is not worth a gate change.
- **Eight of the fifteen file-budget allowlist entries are test files**
  Not a finding, and the arithmetic is wrong twice. scripts/file-budget-allow.txt has 14 entries, not 15; 10 of them are test files, not eight (the title says eight and the body says ten). More importantly the finding concedes its own status - "this is a debt observation rather than a defect" - and the file's own header already says exactly that: "The number is the count it was listed at and the gate treats it as that file's own ceiling, so this is a debt register rather than an exemption. Nothing here is a decision that the file is fine." A per-file ratcheting ceiling that fails on growth and fails on a stale entry is the mechanism working; restating its contents is not a defect to send work at.

## ui-admin-emoji

- **On desktop the card is a phone column stretched to 688dp: a 32-character field and two full-width stacked buttons**
  Rejected on two counts. First, the measurement in the evidence contradicts its own headline: they measured the card interior at 357..1043 (686dp) and the controls at 373..1027 / 377..1023 (~650dp), so the three controls are not 688dp wide - `AppContentColumn` caps at `kContentColumnMax = 720`, the scaffold's `EdgeInsets.all(s16)` and `AppCard`'s own padding take 64 of it, and the controls land near 654. Second and more important, this is the app's settled convention rather than this screen's slip: `full: true` stacked actions inside the 720dp capped column appear in `invites_screen.dart:161`, `channel_overwrites_screen.dart:259` and `:269`, `role_editor_sheet.dart:151`, `create_channel_sheet.dart:133`, `manage_channel_sheet.dart:205` and `:220`, `poll_composer_sheet.dart:136`, `server_fingerprint_step.dart:154`/`:161` and `server_identity_changed_step.dart:99`/`:107`, and `content_column.dart`'s doc explains the cap deliberately. Rebuilding a two-column form footer for the emoji card alone would make it the outlier. If the convention itself is wrong that is a design decision for a settings-wide pass, not an emoji finding.
- **The screen itself has no MANAGE_SERVER guard, so a direct navigation shows a form that can only fail**
  Rejected as filed. The facts are right - `EmojiScreen.build` returns the scaffold unconditionally and the gating is `if (canManageServer)` in `space_settings_section.dart:98-105` - but it is the codebase-wide pattern, not this screen's exception: none of `reports_screen.dart`, `invites_screen.dart`, `roles_screen.dart`, `removed_members_screen.dart` or `channel_overwrites_screen.dart` checks its own entry permission either (the only `Perm.` checks under `screens/admin/` are `invite_role_grant_picker.dart:33` and enumerations of `Perm.editable`), and `router.dart:118-122` registers all of them the same way. Filing it against the emoji screen misattributes an app-wide navigation-gating convention to one file, which is exactly how a finding gets quoted forward as though the emoji screen were uniquely unguarded. The `security` category is also wrong: the server enforces MANAGE_SERVER on both writes, and reading the list is deliberately open to every member (`client_emoji.dart:5-9`), so nothing is exposed - the cost is a wasted tap, not access. If the ergonomics are worth fixing, it is one change across five screens (or a guard in the route builder), not one.

## ui-admin-invites

- **Junk or zero in "Uses allowed" silently produces an unlimited or dead-on-arrival invite (the zero/negative half)**
  Half the finding is refuted by the server. `http/invites.rs:159-161` reads `if req.max_uses.is_some_and(|max| max < 1) { return Err(ApiError::BadRequest("max_uses must be at least 1")); }`, so "0" and "-1" are rejected with a 400 that the client surfaces (as a SnackBar, per the error-grammar finding) rather than creating anything. The claim that "the UI reports success, shows the code, and the code can never be redeemed" is false, as is the derived reading of `is_usable` seeing `uses(0) < 0`: that row never exists. The surviving half - unparseable text falling back to unlimited - is confirmed separately at medium.
- **The screen title aligns with nothing at either width**
  The arithmetic is correct and I reproduced it from source (AppBar leading 56 + default `titleSpacing` 16 puts the title at panelLeft+72, while `AppContentColumn`'s 720 max inside the 860 panel plus the ListView's 16 puts the card at panelLeft+86, giving exactly the 13px measured on desktop and 57px on the phone) - but nothing here is a defect. This is Flutter's stock `AppBar` geometry with a leading widget, unmodified: `app_theme.dart:79` sets only colour and elevation, no `titleSpacing` override, and no rule in docs/design/design-language.md or the design brief asks a title to share an x with body content. Every back-navigable Material screen in every app looks like this, and the proposed fix (custom `title`, `titleSpacing: 0`, a leading width resolving to the content gutter, across all twelve screens) trades a platform convention for a preference. Filing it invites work at a non-problem on twelve screens.
- **An invite code cannot be copied "or even selected" - and "there is no way to get an existing code out of the app at all"**
  The finding survives in reduced form (confirmed above), but two of its load-bearing claims do not. "There is no way to get an existing code out of the app at all - you must create a fresh invite because you cannot read out the old one" is false: the code is rendered in the row as visible text (invites_screen.dart:282-285), and `generate_code`'s alphabet was chosen specifically to survive transcription - "Avoids characters people misread when reading a code aloud or copying it off a screen" (store/invites.rs:83-86). And "the source design's copy-as-primary-on-compact is not partially implemented here; it is absent" cites a spec I could not find and that the brief contradicts: docs/design/design-agent-brief-2026-07-29.md:106-109 records "a found gap: no compact-width design exists for the admin screens". The re-filed version keeps the real defect (no copy affordance and no selection in the persistent list) without either claim.
- **The designed use meter does not exist / "both things the source design asks for are missing"**
  Rejected as a spec claim, kept only as the expiry-urgency proposal (confirmed at low). I could find no design artifact specifying a capacity bar or pip run for an invite: grepping docs/design for "invite" returns only the deliberately-uniform invite-error note and a feature-exploration line about invite scoping, and the brief states the admin screens have a desktop list design only. `AppSlider.meterColor` is a slider internal, not an invite spec. Asserting the design "asks for" a use meter turns a reasonable proposal into an unimplemented requirement, which is exactly how a soft suggestion gets quoted forward as debt.
- **The three form controls share neither a left edge nor a height (the height half)**
  The left-edge half is confirmed and re-filed; the height half is already recorded as a deliberate, known token gap and must not be re-opened as new. `input.dart:100-104` says it in the file itself: "The design's three sizes (32/38/44) do not line up with any existing AppSizes control-height token (controlMd is 34, controlLg is 38, and there is no 32 or 44 step besides rowTouch...). Reported as a token gap rather than bent to fit; these are the design's own numbers." So input-38-versus-button-34 is the design's numbers meeting the token set, decided and written down, not drift. (The separate phone-only issue - that 38 also misses the 44 touch floor while every sibling resolves it - is a different defect and is confirmed on its own.)

## ui-admin-overwrites

- **The two picker rows are raw Material ListTiles, not AppListRow - which costs haptics, the pressed state, the row type size, the icon scale, and 16px of dead tap margin**
  The code claims check out (channel_overwrites_screen.dart:200-208 and 229-240 are raw `ListTile`s inside `AppCard`'s s16 padding, `app_theme.dart` sets no `listTileTheme` or `iconTheme`, so the title takes M3 `bodyLarge` = AppText.body 15px), but the load-bearing sentence in the "why" is false: "These are the only rows in the app that do not tick or show a pressed state". Raw `ListTile` is the established settings-screen pattern - `widgets/personal_account_sections.dart:64,131,168`, `widgets/space_settings_section.dart:58,66`, `widgets/app_info_section.dart:47,59`, `widgets/join_policy_row.dart:71,79,91`, `screens/admin/role_assign_sheet.dart:96`, `screens/admin/overwrite_target_picker_sheets.dart:42,72,104`, `widgets/pinned_messages_sheet.dart:140`. It is also already recorded: `docs/research/nine-specialist-audit-2026-07-29.md:71` lists "the four `ListTile` settings sections that bypass `AppListRow`'s focus ring" as known and not fixed. Filing it against this screen sends work at two rows when the decision is app-wide (the durable fix is a `listTileTheme` and `iconTheme` in `buildTheme`, which the original's last line gets right).
- **In light theme the informational callout and the interactive card are indistinguishable as surfaces**
  The token arithmetic is right (info tone is `fill = Colors.transparent` with `border = borderSubtle`, callout.dart:75-77; `AppCard` is `surfaceRaised` with the same border, card.dart:63-64; raised vs base in light theme is 1.06:1 by my own computation) but the conclusion is not. "Only the radius differs" and "nothing but the chevron distinguishes them" both ignore `AppCard`'s title band: when `title != null` the card renders an uppercase `AppText.label` header in its own padded container with a bottom hairline (card.dart:71-94). In `admin-overwrites-desktop-light.png` the two read as clearly different objects - the callout is a single unfilled block with a leading info glyph, the card is a white body under a labelled "CHANNEL" header strip with a divider and a chevron. There is no confusion to fix here.
- **Six type sizes on one screen, with the two most important pieces of text at the smallest of them**
  Partly wrong and mostly not this screen's decision. The list of sizes is five distinct steps (20 / 15 / 14 / 12 / 11), not six - it double-counts 12px. Two of the three sizes complained about are fixed inside shared components used identically everywhere: `AppCallout` always renders `AppText.caption` (callout.dart:107) and `AppSegmentedControl.inline` always renders `AppText.caption` (segmented_control.dart:133), so the hierarchy claim is an argument against those components, not against this file. The 15px picker row is the rejected raw-`ListTile` finding above. The one screen-local item, the bare `TextStyle(color: tokens.textPrimary)` at permission_overwrite_row.dart:49, resolves through the theme's `bodyMedium` to exactly the intended `AppText.ui` 14px (app_theme.dart:75), so nothing renders wrong today - naming the step is worth doing as hygiene but is not a defect to file.

## ui-admin-reports

- **The app bar title sits 40px right of the content it titles**
  Measurement artifact, and the direction is reversed. They took the body's left edge from `ListView` padding alone (16) and missed `AppContentColumn`, which the shared scaffold wraps the body in (settings_screen_scaffold.dart:54) and which caps the column at `kContentColumnMax = 720` and centres it (content_column.dart:18, 36-41). In an 860-wide desktop modal panel that insets the body to 70 logical, so the reports card's left edge is at 86 and its text at 102 - not 16 and 32. I verified this empirically rather than by arithmetic: their own admin-reports renders are the empty state and contain no card to measure, so I probed space-settings-desktop-light.png, the same scaffold in the same modal with real rows, and the leftmost body ink sits at 87.0 logical (70 column inset + 16 ListTile content padding) across every row. Their title measurement is right - I reproduced it, back-arrow ink 20.0-35.5 and title ink 73.5-143.5 - but that puts the title's box at 72, i.e. within 2px of the content column's own left edge at 70. The title is aligned, and their proposed `titleSpacing: 16` would pull it 56px to the left of the content and create the misalignment they were trying to fix. On the phone renders the body edge really is 16 and the title 72, but there the app bar is the platform's own chrome at Material's default spacing, which is not a defect worth changing on eight screens.
- **Repeated identical Dismiss/Resolve labels give a screen reader no way to tell the reports apart**
  The load-bearing half of the evidence is wrong. They claim "nothing bounding one report from the next", but `AppCard` ends with `return Semantics(label: semanticLabel, container: true, child: card)` (design_system card.dart:97) - `container: true` creates a semantics boundary per card, so each report is announced as one group with the reason and snapshot text as its own nodes ahead of the buttons in traversal order. A screen reader user does not hear five bare "Dismiss, Resolve" pairs; they hear each report's text then its two controls. What is left - that the buttons are not individually named and the card group carries no label - is not independently actionable: there is nothing to name them with, which is the identity finding, and their own direction concedes it ("once identity is available"). Folded into that finding rather than filed as a second one.
- **Filing time is absolute-only, with no age and no ordering cue**
  Contradicts a documented deliberate convention. `formatDateTime`'s own doc comment (client/packages/app/lib/src/format.dart:6-8) says it is "Used wherever a screen shows a Unix millisecond timestamp that is not a message (a report, an invite, a role) and so has no chat-specific 'today' shorthand to reach for" - reports are named explicitly as a case this format is for. There is no relative-time helper anywhere in the app (I grepped for one), so adopting "4 days ago" on this screen alone would either invent a second time treatment for one card or diverge from invites and roles. The queue-count-and-ordering half is a pure enhancement with no defect behind it: the server's "oldest first" contract (schema/openapi.yaml, listOpenReports) is honoured, it is just not narrated. Preference, not a finding.

## ui-admin-roles

- **The role editor's primary action and its error sit below sixteen toggles with no pinned footer**
  The central claim is contradicted by the layout. In role_editor_sheet.dart the error Text (lines 138-144) sits immediately above the Create/Save AppButton (lines 146-154), separated by a single 12pt gap, so a submit failure appears exactly where the user's attention already is - directly above the button they just pressed - not "off-screen". Inserting it pushes the button down by ~28pt; it cannot land outside the viewport while the button is in it. The remaining complaint (16 rows means scrolling to reach Create on a phone) is an ordinary long form, not a defect, and the 0.85 dialog ceiling (sheet.dart:78) plus 30pt pointer-density rows put the whole form inside a 1080-tall desktop window anyway. Their own "any window under roughly 1000pt" figure is derived from the 44pt touch density that only applies below kCompactWidth, where the widget is a scroll-controlled bottom sheet rather than a capped dialog.
- **Every row of member_roles_sheet renders its role name in secondary text, so nothing on the sheet is primary except the header**
  This is AppListRow's documented behaviour for every non-selected, non-unread row in the app, not a defect of this sheet. list_row.dart:26-31 states the rule deliberately ("the source lifts both colour (text-secondary to text-primary) and weight (regular to medium) for selected || unread alike"), and it is what the channel rail's unselected rows and the member pane already look like. Their suggested fix - `selected: held` - would also draw the left accent marker and accent fill that mean "this row is the current selection" and report `Semantics(selected: true)` for a state that is not selection, which is the confusion list_row.dart:41-45 exists to prevent. They themselves measure the contrast as fine (6.2:1 / 7.2:1). Re-filing a design-system convention against one caller is exactly the kind of finding that gets quoted forward as a bug.
- **The roles list never says who holds a role, though the roster is already fetched next door**
  No code behaves incorrectly; this is a feature request, and the stated "one derivation from a provider the screen's own child already reads" is wrong. membersProvider is `listMembers()` with no paging (member_presence.dart:20-22) against a server endpoint capped at MEMBERS_MAX_LIMIT = 200 (mirrored in member_presence.dart:37-40, "at or above it, an unknown id is normal (a member off this page)"), so a count derived from it silently undercounts on any deployment past that page - which is worse than showing no number in a delete confirmation that is meant to quantify risk. Nothing in the reviewed code is broken by its absence.

## ui-channel

- **The near-empty channel leaves 32-63% of the conversation pane blank (as a high-severity defect)**
  The measurement is right and the framing is not. The bottom-anchored short conversation is explicitly deliberate, stated in a code comment on the line the finding cites: `message_transcript.dart:125` reads "Reversed so a short conversation sits against the composer; the start header rides one past the oldest message, which reverse puts at the top." `reverse: true` is what produces the band, and it is the standard shape of every chat transcript (a three-message Discord channel renders the same way). CLAUDE.md also records `ChannelStartHeader` as having been added specifically to "fill the empty band above a short bottom-anchored conversation", i.e. the band was a known accepted condition, not an unnoticed one. A pixel-percentage of emptiness is not evidence of a defect when the anchoring is the design. What did survive from this finding is the narrow, checkable part - the layout flips between zero and one message, and the doc comment overclaims - which I confirmed separately at low.
- **Times are hardcoded 24-hour English with no locale (the locale half)**
  Documented as deliberate with a load-bearing reason at the exact function cited. `message_row_identity.dart:20-22`: "`HH:mm`, as the design uses throughout. Fixed width matters here: a grouped message puts its time in a 36px gutter, and '12:05 PM' wraps to two lines in it while '12:05' does not." So the format is a design decision with a layout constraint behind it, and routing it through `intl` against `Localizations.localeOf` as the finding proposes would break the 36px continuation gutter in every 12-hour locale - the fix is a regression, not an improvement, without a redesign of the gutter first. The finding also asserts intl is "already in the Flutter dependency graph" without evidence that it is a declared dependency of any client package. The genuinely uncovered part - no full date in a tooltip or in semantics - I confirmed separately; that one costs nothing and touches no layout.
- **The composer's boundary is under WCAG 1.4.11's 3:1 (as a medium-severity actionable defect)**
  The arithmetic checks out - I recomputed all four ratios from `app_tokens.dart` and got 1.384 (dark borderSubtle #2E333A on surfaceBase #17191C), 1.103 (raised on base), 1.247 (light #DCE0E5 on #F7F8F9) and 1.06 (white on #F7F8F9), matching the reported 1.38/1.10/1.25/1.06. But the finding rests on the claim that decision 0004 left the question open only for separator hairlines and so does not cover controls, and the proposed fix does not work: the existing stronger token, `borderStrong`, reaches 3.76:1 on dark surfaceBase but only 1.55:1 on light surfaceBase, so a `borderControl` at >=3:1 in light mode lands close to the `#5A5A5A` that `docs/decisions/0004-visual-identity-review.md:51-53` already identified and rejected as "a visible grey rule rather than a hairline". That is the same unresolved trade-off the decision record parked, reached from a different direction rather than a new finding, and shipping the proposed token would change the light theme's entire visual character. Recorded as a real open question for the owner (and worth noting alongside it: the composer draws no focus treatment at all - `composer.dart:337-338` is a static `borderSubtle` and `ComposerField` sets `focusedBorder: InputBorder.none`, which is a 2.4.7 question the finding did not raise), but not as an actionable medium defect.

## ui-onboarding

- **Both entry actions open raw Material AlertDialogs that share no component, type or spacing with the shell behind them**
  The visual half is factually wrong, and the outlier claim is wrong too. buildTheme exists specifically to keep raw Material widgets on-system, and says so: "The component themes below are what keep a raw Material widget on the system when it slips past the `App*` components: without them a bare `FilledButton` is a Material stadium pill and a bare `TextField` an underline" (app_theme.dart:19-25). It sets `inputDecorationTheme` with `filled: true`, `fillColor: tokens.surfaceRaised`, an `OutlineInputBorder` at `AppRadii.control` in `borderSubtle`, a 2px `focusRing` focused border and `dangerBorder` error borders (app_theme.dart:110-140), plus `filledButtonTheme`/`textButtonTheme` with `controlShape` and the app's `buttonLabel` style (lines 90-109). So these dialogs' fields and buttons already carry the app's border-first treatment - there are no Material underlines to see. The quoted sheet.dart line is also read backwards: "Every modal in the app was one" means every modal was a bottom sheet, i.e. the problem showAppSheet fixed was bottom sheets appearing on desktop, and a centred dialog on desktop is exactly what showAppSheet produces. And "this was already fixed everywhere else" is false: `grep -rn 'AlertDialog'` over client/packages/app/lib/src returns four other sites, including the two shared helpers widgets/confirm_dialog.dart:25 and widgets/report_dialog.dart:19, the latter an AlertDialog holding a TextField form. The only residue is that a phone gets a centred dialog rather than a thumb-reach sheet, which is a codebase-wide pattern question and not an onboarding defect - filing it here sends the work at the wrong scope. The one real consequence of using a bare AlertDialog, the missing scroll, is confirmed separately above.
- **The split-panel structure rests on a 1.07:1 fill step, so in light theme it reads as one page with the content pushed right**
  The arithmetic is right - I recomputed it: light #EFF1F3 on #F7F8F9 is 1.065:1 and the #DCE0E5 hairline is 1.247:1; dark #0F1113 on #17191C is 1.074:1 - but the finding is not an independent defect. It says so itself ("This is the same problem as the empty-panel finding seen from the colour side") and its primary direction is the first finding's. What is left is a token change for one divider, and that is a worse idea than it looks: dark `borderStrong` is #6C757E, which against #17191C is a hard, near-4:1 line, so applying it here would make onboarding's divider the strongest edge in the app on a screen whose whole visual argument is quietness. The even surface ramp is also a recorded deliberate choice, and the finding concedes it is not a WCAG failure. Confirming it would double-count the composition problem and attach a token edit to it.

## ui-settings

- **Dead branch on the wide layout, and no empty state if a scaffold is ever given no panes**
  The branch is unreachable and the caller they cite as the risk does not use this widget. `grep -rn 'SettingsPanesScaffold' client/packages` returns exactly one caller, `PersonalSettingsScreen` (personal_settings_screen.dart:40), which passes four literal non-empty `SettingsPaneGroup`s. So `selected == null` on the wide branch (settings_panes.dart:167-171) cannot occur: `selected = wide ? (_selected ?? panes.firstOrNull) : _selected` (:106) and `panes` is never empty. The finding's own justification — "a caller that passes an empty or permission-filtered group list (which is exactly how Space settings is described as behaving for an unprivileged caller)" — is factually wrong: `SpaceSettingsScreen` returns `SettingsScreenScaffold`, not `SettingsPanesScaffold` (space_settings_screen.dart:19), and its permission-filtered empty case is handled by `SpaceSettingsSection` returning `SizedBox.shrink()` (space_settings_section.dart:45-47) in a completely different frame. A defensive `SizedBox.shrink()` guarding an impossible state in a widget with one caller is not a defect, and adding an empty-state message for a caller that does not exist is speculative work. If Space settings is later moved onto this scaffold (see the two-settings-languages finding), the empty case becomes worth handling then.

## ui-signin

- **Desktop leaves about half the window empty, and in light theme the panel that justifies the two-column layout is imperceptible**
  Both halves restate decisions that are already on the record as deliberate, and the finding itself defers to the owner rather than naming a defect. The panel's emptiness is documented verbatim in `_BrandPanel`'s own doc comment (onboarding_shell.dart:142-151: "Deliberately blank below the wordmark... The layout is kept rather than collapsed so there is a place to put real words when there are some") and the finding agrees the reasoning is sound. The 1.06:1 surfaceSunken-to-surfaceBase step is not an onboarding choice: it is the system-wide surface ramp every rail in the shell uses, governed by `surface_ramp_test.dart` and by the explicit border-first elevation decision in app_tokens.dart:16-18, and contrast_test.dart:62-77 records the border ratio as reported-not-gated with the reasoning written out. Filing a system-wide token decision as a sign-in bug sends work at the wrong file. The remaining piece - whether `Alignment(0, -0.55)` leaves too much void - is an aesthetic judgement I cannot evidence without regenerating renders, and the finding's own direction says it "needs the owner's eye rather than a solo change".
- **An invite-only Space tells you to leave the screen to redeem a code the submit path already accepts**
  Not a defect. `_submit` does pass `inviteCode` (183-191) from `pendingInviteProvider`, and there is no invite field on this screen - both true - but the detour is the documented intended route, not an oversight: the comment at sign_in_screen.dart:375-376 says "Once a Space is remembered, sign-in is where a signed-out user lands, so this is the only way back to invite redemption", and `initState` (67) already opens the screen in create-account mode when a pending code exists, so the arriving-with-a-code case is handled. The notice copy is accurate about where to go. Adding an invite field here is a reasonable feature proposal, but nothing is broken, no path fails, and filing it as a medium-severity finding overstates it.
- **Onboarding is the only flow in the app still using raw Material TextFields instead of AppInput**
  Already recorded as known outstanding work, with the blocker named: docs/research/nine-specialist-audit-2026-07-29.md:143 lists "the sign-in and onboarding fields moving onto `AppInput` (needs `helperText`/`autofillHints` passthrough first)" among items recorded-but-not-done. Re-filing it is the exact cost CLAUDE.md warns about. Two evidence errors besides: the title's "only flow" is wrong on its own grep - `TextField(` appears in five files including report_dialog, message_edit_field and composer_extras - and `AppInput`'s heights are 32/38/44 (components/forms/input.dart:105-109), not the "32/34/38" the finding states, which changes its own conclusion about height being the one thing the raw field does better.

## ui-space-settings

- **Desktop wastes about half the panel: fixed 72pt rows ignore the design system's density pair, and a 360pt text column sits in an 860pt panel**
  The density half is a measurement artifact of the snapshot harness. `main.dart:114` wraps the entire app in `ListTileTheme.merge(dense: !AppTouchTargets.of(context))`, with a doc comment stating exactly the problem they are re-filing: "Material's own list rows are sized for a thumb whatever the window, which is what made the settings and administration screens read as a phone blown up. Applied here rather than per screen so a new one cannot forget, and read from the window so nothing has to say which." `AppTouchTargets.of` falls back to window width against `kCompactWidth = 600`, so the shipped desktop app renders these rows dense. The harness (ui_snapshot_test.dart:96-107) builds a bare `MaterialApp.router` with no `builder`, so the 72.0pt pitch they measured exists only in the PNG. Their proposed remedy is also wrong in kind: `AppListRow` has no subtitle slot at all (label, leading, meta, trailing, selected, unread, muted, touch, height, onTap) and `AppSizes.rowPointer = 30` cannot hold a two-line row, so converting would delete the subtitles - which their own summary calls the best copy in the app. The horizontal half misapplies `content_column.dart`'s doc comment: that comment is about a label at one end and its VALUE at the other, and the thing 368pt away here is a chevron, a pure disclosure affordance at the trailing edge by universal convention, not information to pair with the label. The only real residue is that the panel is a fixed 720pt with 178pt (25%, not "roughly half") empty below the content, which is confirmed separately as the fixed-panel-height finding.
- **Seven flat peers with no grouping and no state (the grouping half)**
  The absence of a group header is deliberate and documented at the top of the file they cite. space_settings_section.dart:6-7: "This is [SpaceSettingsScreen]'s whole body, not a section sharing a screen with personal settings, so it owns no group header or divider of its own." Comparing it to personal settings' `SettingsPaneGroup` labels is comparing a body to a nav: personal settings is a nav-and-pane scaffold where the groups label nav entries, and this screen has no nav. Their own summary concedes the link-list shape is "a defensible shape" because the six destinations are full CRUD screens with their own app bars and `modalPage`, which is the correct read. The `SettingsPane.badge` evidence is also misattributed - `badge` is a field on the personal-settings nav's `SettingsPane`, a widget this screen never constructs, so it being "unreachable from here" is trivially true and not a defect of this screen. The one substantive part, that Reports carries no count of pending items, survives as its own narrowed finding.
- **"Who can join" is visually the odd row out because its subtitle names its colour**
  Backwards, and it double-counts. This is the one row on the screen whose subtitle is the CORRECT colour: `tokens.textSecondary` is #A7AEB6 dark / #5B6169 light and that is exactly what line 96 sets and what I measured (633px of it in each render). The six neighbours are the wrong ones, painting Material's `onSurfaceVariant` at #C0C8CD / #40484D because there is no ListTileTheme - which is the ListTile-colour finding, already filed. Filing this as "the odd row out" would send someone to delete the one correct colour on the screen. Their direction even says so in passing ("or, better, fix all seven at once") while the title and severity say the opposite. Their measurement direction is also wrong: they call it "visibly lighter in dark", but #A7AEB6 is darker than #C0C8CD, so it is dimmer in both themes, not lighter in one. The row rendering as a `ListTile` rather than `SettingsSelectRow` is separately deliberate and documented twice (join_policy_row.dart:4-8 and settings_select_row.dart:48-50). The copy half - two names for one setting, mixed subtitle modes - survives as its own finding.
- **A failed join-policy change vanishes as a SnackBar, a 28th site**
  Not a defect of this scope, and the "28th" framing is wrong. `showSnackBar` has 26 call sites across 16 files in the app package, including the mutation-failure paths of roles_screen, invites_screen, reports_screen, emoji_screen, channel_overwrites_screen, member_actions, presence_menu, avatar_settings_section and composer. So a toast on a failed write is the app's prevailing pattern, not a lone omission - the `AppErrorState` sweep CLAUDE.md records converted failures that were only ever a toast on load and read paths, and `error_state.dart`'s own doc comment names the cases it was written for ("a failed account deletion or a failed block"). Whether mutation failures should also be persistent is a real app-wide question, but singling out one of 26 as a space-settings finding would send work at one line and leave 25 peers, and would be quoted forward as though this screen were the outlier. Worth noting the cited path is not silent either: it records to `debugLogProvider` first (join_policy_row.dart:53-55), which is more than most of the 26 do. File it against the pattern or not at all.
- **The near-empty panel's zero-permission case is unreachable in practice (their reachability reasoning)**
  Recording this as a partial rejection because the finding survives but its reachability argument does not, and the corrected version is materially different. They argue reachability turns on "/settings/space is a plain path in a web build, so a bookmark or a pasted URL reaches it" - true but marginal. The stronger fact they missed is that `myPermissionsProvider` is `ref.watch(meProvider).valueOrNull?.permissions ?? 0` (admin_providers.dart:25-27), so the same empty branch is the LOADING state, and every cold navigation renders the titled empty panel until /me answers regardless of who is looking. The confirmed finding carries the corrected reasoning.

## ui-voice

- **The in-call control bar's only visually bounded button is the destructive one**
  The premise is wrong on the code. All three `_ControlButton`s draw `border: Border.all(color: border)` (voice_call_controls.dart:171-175); the destructive one differs only in *which* border token it gets, so it is not the only bounded one. The mic button is also `active` in its normal state, taking `accentSoft` fill with `accent` foreground (:146-155), so 'mute and share read as bare floating icons' describes the muted/not-sharing case only. What remains is that `borderSubtle` sits ~1.25:1 against `surfaceSunken` - but that is the design language's border-first elevation used identically on every bordered surface in the app (app_tokens.dart:168, and see the trueBlack doc comment explaining the hairline choice deliberately), so filing it against the voice bar specifically is filing the palette. They also concede it is not a 1.4.11 failure, and unlike their other visual claims this one was computed rather than sampled, because no in-call render exists.
